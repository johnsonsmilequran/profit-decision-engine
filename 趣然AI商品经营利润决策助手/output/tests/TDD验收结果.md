# TDD 验收结果 · 趣然 AI 商品经营与利润决策助手

- contract_sha256: sha256:0b66fa7a4214b8e9a0198811f74c7ed3219618ddfa9ce436339cb2b022727df9
- run_started_at: 2026-08-04T19:19:45+0800

| TDD ID | 状态 | 动作 | 证据 |
| :--- | :--- | :--- | :--- |
| SMOKE-01 | pending | 待发布候选执行完整安装、构建、Compose 与健康检查 | — |
| SMOKE-02 | pending | 待发布候选执行真实数据核心旅程 | — |
| FLOW-01 | pending | 已完成钉钉 OAuth HTTP 契约、失败默认拒绝、真实 PostgreSQL 双角色/无角色/会话失效矩阵与站内安全返回地址验证；等待钉钉测试应用及浏览器 E2E | `TEST_DATABASE_URL=postgres://…@127.0.0.1:55434/profit_decision?sslmode=disable go test -race ./internal/identity -count=1 -v` exit 0；精确验证授权 URL 参数、token POST 与 unionId GET，401/403、缺 token/unionId、非法 JSON 和缺部署配置全部拒绝；运营会话、主管新会话、无角色、角色变更、停用、撤销、闲置 2h 与绝对 12h 失效均由 PostgreSQL 判定；`go test -race ./internal/httpapi -run TestSafeReturn -count=1 -v` exit 0，绝对 URL、`//` 与 OAuth 回调返回地址均降为 `/`；未提供真实钉钉测试应用且浏览器不可用，故不记 pass |
| FLOW-02 | pass | 对真实 XLSX 的合法导入、幂等/并发、非法期间、身份缺失、重复 SPU 与经营字段降级执行 API/数据库测试，并在 RC2 浏览器复核批次状态和持久化详情 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 TEST_XLSX_PATH=…/商品链接.xlsx go test -race -count=1 -v ./internal/batch` exit 0；8 路并发仅 1 个非幂等结果、同一 batch_id、批次/清单 1/1，非法期间持久化 0，重复/身份错误拒绝且经营异常保留定位；RC2 生产 API 导入生成 `BATCH-20260630-4EC92F6A`，浏览器 `/batches` 与 `/batches/4ec92f6a-a1e2-4317-abda-551cd1187c56` fresh 复核 1 批次、10 有效/0 拒绝/10 降级、文件 `商品链接.xlsx`、10 条真实 SPU 与逐字段问题证据 |
| FLOW-03 | pass | 执行指标边界、异常与汇总行测试，并在真实批次证据抽屉和建议详情复核来源、原值、周期及缺失语义 | 同一 `go test -race -count=1 -v ./internal/batch` exit 0；品退率=1/100=0.01、库存天数=(100+20)/(70/14)=24、源利润率=0.1，零分母/负库存/非法利润为 nil+独立质量态，汇总行排除且差异告警；RC2 浏览器批次详情打开真实证据抽屉，显示源行、原始列值、结构化规则证据和影响；建议详情 `/suggestions/2b15249e-fb77-4fab-b214-59e26364b438` fresh 显示销售 `¥56,461.5`、利润率 `7.60%`、近 7 日品退率“未校验”、库存天数“数据不足”，未补 0 |
| FLOW-04 | pass | 先红后绿执行全分类/阈值/动作/缺字段矩阵及数据库原子组合，并在真实清单与详情复核规则结果 | 旧提交 `6e45b81` 的 3 个 `watch` 分支 `go test` exit 1；当前 `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 TEST_XLSX_PATH=…/商品链接.xlsx go test -race -count=1 -v ./internal/batch` exit 0，13 个边界+5 个证据不足分支全过，真实批次补货/禁补矛盾=0、遗留 watch=0；RC2 浏览器行动中心固定顺序显示 2 条清仓后 1 条止损，详情分别展示冻结触发规则与 `清仓/止损 + 禁止补货`，真实 10 决策/7 任务均来自 `RULE-V1.0` |
| FLOW-05 | pass | 执行 LiteLLM HTTP 故障/越界矩阵并在未配置模型的真实浏览器降级态继续完整人工经营闭环 | `go test -race -count=1 -v ./internal/explanation` exit 0，覆盖拒绝连接、超时、502、非法 JSON、缺字段、动作冲突、虚构数字、退款归因和恢复，失败版本不保存越界内容且业务状态不变；RC2 浏览器建议详情 fresh 显示“模型服务未配置”，同时主管审核、运营执行、结果记录、清仓提交及主管确认均成功，冻结销售/利润/规则动作始终未变 |
| FLOW-06 | pass | 执行页签/组合筛选/角色范围/分页状态机，并以最新 RC2 镜像完成运营与主管双角色浏览器 E2E | `TestActionTabsAndFiltersFollowRoleProgress` 与独立 PostgreSQL `go test -race ./internal/action -count=1` exit 0；RC2 运营工作台 fresh 显示真实批次、本人 4 个 SPU 的两类数据限制各影响 4 个；行动中心四页签为待我处理/全部/处理中/已完成，`search=745838943806&action=stop_loss` 唯一返回真实 SPU 且 URL 保留条件；主管工作台显示 7 条待审核且仅有逐条入口、无批量写；执行后待我处理、状态与生命周期时间同步更新 |
| FLOW-07 | pending | 已完成真实 PostgreSQL 三周连续批次的稳定任务、同动作续接、动作变化待审、版本/状态不重置及最近前序冻结快照验证；等待运营/主管浏览器跨周 E2E | `TEST_DATABASE_URL=postgres://…@127.0.0.1:55434/profit_decision?sslmode=disable go test ./internal/batch -run TestActionLifecycleContinuesStableTaskAndStagesChangedAction -count=1 -v` exit 0；三周仅 1 个稳定任务、3 条周关联、2 个动作版本；第二周审核版本 4、经营版本 3、库存版本 5、经营已记录结果/库存已处理及执行时间均未重置；第三周仍以清仓为当前生效动作并新增观察待审版本，`previous_link_id` 精确指向第二周，前序净销售额 2222、利润率 -0.22、品退率 0.022、库存天数 22、触发规则及经营状态均来自第二周冻结记录；浏览器不可用，故不记 pass |
| FLOW-08 | pass | 执行审核状态机、幂等/并发与错误响应，并由主管浏览器逐条通过真实双轨建议 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 go test -race -count=1 -run 'TestSupervisorReview|TestSupervisorRejection|TestErrorResponses' -v ./internal/action ./internal/httpapi` exit 0，覆盖通过、驳回必填、无权、重复、旧版本 409 与最新操作者/时间；RC2 主管浏览器 `/suggestions/8749b4e8-46aa-4af1-92ac-e4aa983c4765` fresh 提交备注并确认整体通过，状态 `pending→approved`，经营/库存同时 `pending_execution`，时间线仅追加 1 条 `suggestion_review`，运营随后才获得执行入口 |
| FLOW-09 | pending | 已完成服务端人工改判字段存在性、动作变化、空理由、沿用禁补、旧版本、三种库存选择、已执行先终止和追加审计验证；等待主管建议详情浏览器弹窗闭环 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 go test -race -count=1 -run 'TestSupervisorOverride' -v ./internal/action` exit 0；`TestSupervisorOverrideRequiresChangedActionAndExplicitInventoryChoice` 的 restock/no_restock/no_coordination 3/3 pass，非法请求事件数 0 且任务保持清仓+禁补；每个合法请求原固定规则仍为清仓+禁补、生效版本唯一、审计前后动作逐项一致；`TestSupervisorOverrideRequiresTerminationAfterExecution` pass；浏览器返回 No browser is available，故不记 pass |
| FLOW-10 | pass | 执行双轨独立推进、对象授权、幂等/旧版本、OA 成败与结果持久化，并完成真实浏览器经营轨/结果/OA 失败闭环 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 go test -race -count=1 -run 'TestOperationalCommands|TestOAFailure' -v ./internal/action` exit 0，OA sent 后协同仍待核验，结果值/周期/记录人/备注持久化且失败可重试；RC2 运营浏览器 fresh 将真实任务经营轨 `pending_execution→executed→result_recorded`，记录 2026-08-04 周期且三类值均明确“未提供”；OA 表单保留接收人/反馈要求，未配置适配器生成持久化 `failed(oa_not_configured)` 且库存轨仍 `pending_execution`，未伪造外部反馈 |
| FLOW-11 | pass | 执行跨日催办唯一性、失败补发与三类停催条件，并完成运营提交/主管确认的浏览器闭环 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 go test -race -count=1 -v ./internal/action` exit 0；两个 Asia/Shanghai 自然日恰有 2 条通知、3 次发送尝试，确认后新增 0，改判/终止停催 2/2 pass；RC2 浏览器 fresh 中运营提交 `2026-08-04T21:45` 与验收说明，状态 `pending_confirmation`，主管逐条查看并确认后经营轨变 `closed`，时间线追加提交与复核事件；催办失败明确显示 `oa_not_configured` 且不伪造送达 |
| FLOW-12 | pass | 执行历史组合检索、快照重放、角色裁剪与不可变更新拒绝，并完成浏览器组合查询和只读详情 E2E | `TEST_DATABASE_URL=postgres://…@127.0.0.1:55434/profit_decision?sslmode=disable go test -race ./internal/action -count=1` exit 0，覆盖改判后冻结 RULE-V1.0、通过/驳回/部分执行/冲突/AI 失败投影、原位更新拒绝及非责任运营 total=0；RC2 浏览器 `/history?search=683939339441&action=clearance&review_status=approved&execution_state=closed` fresh 唯一返回 1 条，进入 `mode=history` 后保留批次/期间/规则/销售/利润/清仓+禁补及全部事件，返回 URL 恢复四项条件，页面明确只读且无审核、执行、结果或 AI 重试入口 |
| DESIGN-01 | pass | 以 HTML 明确呈现的 1440×900 登录状态生成 baseline/actual，并在修复范围标识、首屏裁切和标题层级后由独立视觉验收官实际看图复核 | `baseline=/Users/peter123/Documents/work/profit-decision-engine/tests/visual/PAGE-F07-01-baseline.png; actual=/Users/peter123/Documents/work/profit-decision-engine/tests/visual/PAGE-F07-01-actual.png; reviewer=independent-visual; review=pass; blocking=0`；actual 使用同尺寸、同文案和真实钉钉授权 URL 二维码，真实二维码图案差异不作为阻塞 |
| DESIGN-02 | pass | 以 HTML 明确呈现的 1440×900 会话失效恢复状态生成 baseline/actual；首次独立复核发现 2 项阻塞后改用 SVG 语义图标并恢复双端安全页脚，重建重截并由独立视觉验收官再次实际看图 | `baseline=/Users/peter123/Documents/work/profit-decision-engine/tests/visual/PAGE-F07-02-baseline.png; actual=/Users/peter123/Documents/work/profit-decision-engine/tests/visual/PAGE-F07-02-actual.png; reviewer=independent-visual; review=pass; blocking=0`；actual sha256=`458408b58e4a642f4794ee0b836b84203a4b7860ec5b1854d0e4f3162c254606` |
| DESIGN-03 | pending | 待生成同状态截图并由独立视觉验收官看图 | — |
| DESIGN-04 | pending | 待生成同状态截图并由独立视觉验收官看图 | — |
| DESIGN-05 | pending | 待生成同状态截图并由独立视觉验收官看图 | — |
| DESIGN-06 | pending | 待生成同状态截图并由独立视觉验收官看图 | — |
| DESIGN-07 | pending | 待生成同状态截图并由独立视觉验收官看图 | — |
| DESIGN-08 | pending | 待生成同状态截图并由独立视觉验收官看图 | — |
| DESIGN-09 | pending | 待生成同状态截图并由独立视觉验收官看图 | — |
| RULE-01 | pass | 对 4 组冻结快照执行每组 3 次串行、24 次并发重放，覆盖销售/利润/新品阈值边界；真实 XLSX 批次完成后重建 Worker 3 次，验证不重领完成任务且冻结决策 JSON 逐字节一致 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 TEST_XLSX_PATH=…/商品链接.xlsx go test -race -count=1 -v ./internal/batch` exit 0；`TestDecisionRuleBoundaries` 4/4 子用例 pass；`TestDecisionIsStableAcrossSerialAndConcurrentReplay` pass；`TestBatchLifecycleAgainstPostgresAndRealWorkbook` pass（3 次 Worker restart 均 `processed=false`，10 条决策重放前后 JSON bytes 相等） |
| RULE-02 | pending | 已补齐 OA 独立白名单 DTO、稳定任务引用、真实 PostgreSQL 消息快照与 HTTP 出站敏感字段负向扫描；等待浏览器遍历全部页面/错误/历史路径及公司 OA 正式回执 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 go test -race -count=1 -v ./internal/oa ./internal/action` exit 0；`TestClientSendsOnlyNotificationWhitelist` 精确断言 7 个字段且扫描利润/推广/品退/售后/阈值/审核为 0；`TestOAFailureCanRetryWithoutChangingInventoryState` 真实 PostgreSQL 持久化与 HTTP 接收负载的 `spu_id/task_reference/action/operator` 逐项一致；浏览器与公司 OA 尚无可用外部资源，故不记 pass |
| RULE-03 | pending | 待执行 | — |
| RULE-04 | pass | 对 LiteLLM 全故障矩阵执行失败关闭，并在真实未配置状态继续审核、执行、结果和清仓确认闭环 | `TestClientFailsClosedForGatewayAndContentFaults` 8/8、`TestClientFailsClosedForConnectionRefusalAndTimeout`、`TestProcessorAppendsFaultVersionsWithoutChangingFrozenDecisionOrTask` 4/4、`TestProcessorPersistsValidatedExplanationWithoutChangingBusinessState` 全部 pass；RC2 浏览器建议详情始终显示受控“模型服务未配置”，从主管审核到经营轨 closed 的全链路成功，冻结 `RULE-V1.0`、触发规则、销售 `¥59,930.95` 与利润率 `4.86%` 未改变，未展示密钥或越界内容 |
| RULE-05 | pass | 建立真实导入至闭环事件链，验证跨周前序/改判集成测试、不可变约束，并把 PostgreSQL 与 XLSX 快照恢复到全新 Compose 项目后从生产 API 读取 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 TEST_XLSX_PATH=…/商品链接.xlsx go test -race ./...` exit 0（含跨周关联、改判、审核、执行、结果、清仓、OA 与冲突用例）；候选真实链=1 batch/10 snapshots/10 decisions/7 tasks/7 links/23 events/1 result/1 clearance/1 OA；`backup.sh` 生成 `database.dump+import-files.tar.gz+manifest+SHA256SUMS`；`restore.sh` 三项摘要 OK 且 `restore_status=complete batch_count=1 xlsx_file_count=1`；源/恢复上述 9 组计数逐项相等，恢复 API `/api/history?...683939339441` HTTP 200、audit_count=2、closed/processed；恢复 XLSX sha256=`af37a29543bf5a37117985e70619bee2534190a22655359d6e005ea80058cf5f`；原位 UPDATE snapshot 与 DELETE event 均 exit 1 `immutable batch artifact cannot be changed`；二次恢复 exit 3 `restore target table public.role_mapping is not empty`；本轮恢复耗时秒级，恢复点为本轮即时快照 |
