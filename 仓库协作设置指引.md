# 仓库协作设置指引

仓库：`johnsonsmilequran/profit-decision-engine`

## 当前协作基线

- 默认分支：`main`。
- 协作模式：内部团队优先，外部贡献可使用 fork + PR。
- CODEOWNER：`@johnsonsmilequran`。
- 当前仓库只有产品文档和演示产物，尚无可运行测试栈，因此暂未生成 `.github/workflows/ci.yml`。
- `gh` 当前未登录；以下设置不自动执行。

## 建议的 main 分支保护

仓库页进入 **Settings → Branches → Add branch protection rule**，Branch name pattern 填 `main`：

1. 勾选 **Require a pull request before merging**。
2. 团队有第二位 reviewer 后，将 Required approvals 设为 `1`；仅一位维护者期间不强制外部批准，避免无法合并。
3. 勾选 **Block force pushes**。
4. 暂不勾选必过 CI；建立真实测试命令并新增 CI workflow 后，再启用 required status checks。
5. 需要更严格治理时，可勾选 **Do not allow bypassing the above settings**。

## 后续启用 CI

出现真实技术栈和测试命令后，再按实际项目生成 CI：

- Node：锁文件安装依赖后运行真实 `test` 脚本；
- Python：安装项目依赖后运行 `pytest`；
- 其他语言：使用对应原生测试命令。

CI workflow 的名称建议固定为 `CI`，启用后在分支保护中要求该检查通过。

## 建议标签

| 标签 | 用途 |
|---|---|
| `needs-triage` | 新 issue 待分类 |
| `bug` | Bug |
| `feature` | 新功能 |
| `enhancement` | 现有能力改进 |
| `docs` | 文档改动 |
| `priority:high` / `priority:low` | 优先级 |
| `needs-prd` | 需要进入正式 PRD 流程 |
