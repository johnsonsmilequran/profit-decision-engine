# 趣然 AI 商品经营与利润决策助手技术方案

> 生成时间：2026-08-04  
> 基于 PRD：`PRD详细版.md`、`requirements.md` revision 10、已确认《设计决策蓝图》
> 技术方案版本：v1.7

## 1. 产品技术画像

本产品是玩具事业部内部使用的单端 PC Web 工作台。它把每周 XLSX 固化为不可变 SPU 快照，用固定规则生成经营/库存双动作；只有运营和运营主管登录产品，责任运营通过钉钉企业内部机器人单聊协调相关人员，清仓完成时间经主管确认后闭环。技术方案优先保证确定性、双角色权限、通知幂等和历史可重放。

### 1.1 产品形态

- 产品类型：内部 PC Web App。
- 主要入口：公司钉钉工作台或普通桌面浏览器。
- 端的数量：单端；`1024px` 以下不提供完整经营操作。
- 首轮规模：一个事业部、10—20 个真实 SPU；该数量是验收样本，不是运行硬上限。
- 数据节奏：每周一人工导入新表；周内更新审核、执行和结果状态。

### 1.2 核心技术约束

| 约束 | 来源 | 技术含义 |
| :--- | :--- | :--- |
| 同一快照和规则版本必须结论一致 | PRD §3、NFR-001 | 规则必须是版本化纯函数；不能依赖访问时间、AI 或可变外部数据 |
| 止损/清仓与禁补必须原子生成 | PRD §3.5、REQ-F03-03 | 决策记录和两个动作槽在一个数据库事务内提交 |
| AI 失败不阻塞 | REQ-F04-01～03、NFR-004 | AI 任务异步；结构化四要素和人工闭环先就绪 |
| 双角色与 OA 最小可见 | REQ-F07-01～03、NFR-002 | 业务接口仅接受运营/主管；OA 使用消息白名单，外部相关人员无产品访问权 |
| 批次、规则和事件不可覆盖 | REQ-F08-01～02、NFR-005 | 快照与事件追加写；纠错形成新事件，不做原位修订 |
| 重复导入和状态重试必须幂等 | AC-F01-02、AC-F06-06 | 批次指纹、业务唯一键、幂等键和 version 乐观并发 |
| 同 SPU 同动作跨周不重建任务 | REQ-F05-04 | 决策快照与稳定任务分表；每轨比较动作指纹，相同则关联，变化则追加待审版本 |
| 最近前序建议快照与经营状态筛选 | REQ-F05-05 | `business_unit_id + spu_id` 精确锚定；周度关联保存前序指针和关联时间；由前序指针联查原批次 `decision_record + spu_snapshot + action/event` 投影主动作、关键依据、库存语境和经营状态，禁止用当前批次回填；查询独立过滤当前经营动作轨状态 |
| 清仓完成确认与每日催办 | REQ-F06-05 | 运营提交实际完成时间、主管确认后闭环；按任务/自然日/接收人建立通知幂等键，确认/终止/改判后停止 |
| 国内网络可达 | 蓝图 §8 | 自托管在公司内网或国内云；不依赖海外 PaaS、字体和运行时 |
| 会话最长 12h、闲置 2h | 蓝图 §9 | 服务端会话表记录绝对到期和最后活动时间；每请求复核角色 |
| RPO≤24h、RTO≤4h | 蓝图 §9 | 每晚数据库、XLSX、元数据和配置清单异机备份；季度恢复演练 |

### 1.3 团队与规模画像

- 团队规模假设：2—5 人的小型产品研发团队。
- 技术栈偏好：用户已确认 Go 后端 + React 前端；前端继续使用 TypeScript，后端 API 与 Worker 使用同一 Go Module。
- 用户规模预期：事业部内部早期工具，低并发、历史持续增长。
- 上线压力：以首轮真实 SPU 验证为目标，优先交付完整闭环。
- 运维能力：可提供 Linux 主机、Docker Compose、钉钉配置、LiteLLM 部署密钥和异机备份目标。

### 1.4 非功能性需求

- 一致性：批次、决策、审核和状态事件使用 PostgreSQL 事务保证。
- 安全：默认拒绝；页面、接口、错误和审计都使用同一权限投影。
- 可恢复：AI、网络和并发失败保留确定性结果与未提交输入。
- 可审计：每次成功变化记录操作者、时间、前后版本和备注。
- 可运维：结构化日志保留 90 天；备份保留 30 日备份和 12 月归档。
- 性能：首轮规模不需要缓存或搜索引擎；分页、索引和批次查询覆盖历史增长。

### 1.5 激活维度与决策深度

| 维度 | 是否激活 | 决策深度 | 原因 |
| :--- | :---: | :--- | :--- |
| 前端、后端、主数据库 | 是 | 用户已在蓝图确认，按直接默认落地 | 技术方向已拍板，不重新制造候选 |
| AI / LLM | 是 | 直接默认 | 已有 LiteLLM，只需单轮结构化解释，不需 Agent/RAG |
| 文件 | 是 | 直接默认 | 单实例、持久化磁盘、周度 XLSX、无直传/CDN需求 |
| 认证 | 是 | 直接默认 | 唯一入口为钉钉，无自建账号体系 |
| 可观测、备份 | 是 | 直接默认 | 目标和责任已确认，不引入新 SaaS |
| 实时通信、支付、多端 | 否 | 不写实现 | requirements 没有对应能力 |

## 2. 技术栈总览（一张图）

```text
┌────────────────────────────────────────────────────────────────┐
│ PC Web     React 19.2.8 + TypeScript 5.9.3 + Vite 8.2.0        │
├────────────────────────────────────────────────────────────────┤
│ Web/API    Go 1.26.5 + chi 5.3.1，服务端认证、查询与命令接口     │
├────────────────────────────────────────────────────────────────┤
│ Worker     Go 1.26.5，同 Module 独立进程，规则/OA/AI 异步任务    │
├────────────────────────────────────────────────────────────────┤
│ Data       PostgreSQL 18.4 + pgx 5.10.0 + goose 3.27.3          │
├────────────────────────────────────────────────────────────────┤
│ Import     Excelize 2.11.0 + Go 领域校验，标准化快照与字段质量   │
├────────────────────────────────────────────────────────────────┤
│ AI         现有 LiteLLM HTTP 网关，结构化单轮解释，无 Agent/RAG  │
├────────────────────────────────────────────────────────────────┤
│ Runtime    Nginx + Docker Compose 5.1.2 + 持久化 Linux 主机      │
└────────────────────────────────────────────────────────────────┘
```

### 2.1 模块拓扑

```mermaid
flowchart TB
    WEB["React PC Web"] --> NGINX["Nginx 同源入口"]
    NGINX --> API["Go API / chi"]
    API --> AUTH["钉钉认证与权限模块"]
    API --> CMD["审核/执行命令模块"]
    API --> QUERY["双角色投影查询模块"]
    API --> OA["钉钉企业内部机器人单聊"]
    API --> DB[("PostgreSQL 18")]
    API --> FILE["持久化 XLSX 目录"]
    WORKER["Go Worker"] --> JOB["数据库任务表"]
    WORKER --> IMPORT["XLSX 导入适配器"]
    WORKER --> RULE["版本化固定规则"]
    WORKER --> LLM["LiteLLM 适配器"]
    IMPORT --> DB
    RULE --> DB
    LLM --> DB
    OA --> JOB
    DB --> BACKUP["公司受控异机备份"]
    FILE --> BACKUP
```

### 2.2 模块化单体边界

| 模块 | 责任 | 禁止越界 |
| :--- | :--- | :--- |
| `web` | React 路由、查询状态、表单和角色视图；消费 OpenAPI 生成类型 | 不持有权限真相，不直接访问数据库或外部服务 |
| `identity` | 钉钉回调、会话、角色映射、对象授权 | 不配置业务角色审批，不保存钉钉密码 |
| `imports` | 文件指纹、解析、字段映射、错误和快照 | 不直接产生 AI 结论 |
| `metrics` | 自然月、7 日、14 日指标与质量状态 | 不用缺失值补 0 |
| `rules` | 分类、主动作、库存动作和规则证据 | 不调用 LiteLLM，不读取当前日期重算历史 |
| `explanations` | 四要素兜底、LiteLLM 白名单、越界过滤 | 不修改规则结果 |
| `workflow` | 建议审核、动作执行、结果与 version | 不二次创建规则动作 |
| `notifications` | OA 消息白名单、收件人、按自然日幂等、失败补发 | 不把发送成功写成业务确认，不向外部人员开放产品会话 |
| `history` | 快照、业务时间线、审计事件与角色裁剪 | 不覆盖旧事件 |

## 3. 前端技术选型

### 3.1 推荐：React 19.2.8 + TypeScript 5.9.3 + Vite 8.2.0

React 是用户已确认的实现边界；在 Go 独立后端前提下，采用 Vite 构建客户端 PC Web 是直接默认，不再引入兼任服务端的 React 元框架。

**决策证据**：

1. 产品只有无需 SEO 的内部 PC Web，页面全部在有效钉钉会话后访问，客户端渲染足以满足首屏与交互需求。
2. Go 已承担服务端接口和认证回调；Vite 静态构建避免在生产环境继续维护 Node 服务端运行时。
3. 行动清单、详情和工作台是典型 React 数据界面，生态与 AI 编码语料成熟。
4. 前后端保持同一仓库、同一 Compose 和同源域名交付，不演变为两个独立产品或跨域部署。

**核心配置建议**：

- 浏览器原生 History/Location 组织登录、工作台、批次、行动和历史静态路由；已知 React Router 7.x 安全公告无法在当前版本范围内同时规避，故不引入该依赖；客户端路由只负责体验门禁，Go API 每次请求仍重新鉴权。
- TanStack Query 5.101.4 管理服务端查询、失效与冲突后刷新；URL 保存批次、页签、搜索、筛选和页码，不引入额外全局状态库。
- 版本化 OpenAPI 3.1 为跨端接口契约，使用 openapi-typescript 7.13.0 生成 TypeScript DTO；前端不得手写另一套同名业务枚举。
- 表单使用 React 受控状态和 TypeScript 前端校验；Go 端执行权威校验，网络/冲突失败保留本地输入。
- 页面样式以 `tokens.css` 为唯一视觉契约，生产代码不依赖 Tailwind CDN。
- PC 断点按蓝图执行；小于 1024px 进入受控提示。

**当前代价**：

- 首屏必须先下载静态前端并调用 Go API，不具备服务端渲染；内部工具可接受，但需要路由级代码分割和真实首屏烟测。
- Go 与 TypeScript 不共享源码类型；若跳过 OpenAPI 生成和契约测试，字段、空值和枚举会发生跨端漂移。

**升级触发条件**：

- 若未来出现公开页面 SEO、服务端首屏或边缘渲染的稳定需求，再重新评估 React 元框架；MVP 不提前引入。
- 若客户端出现无法由 URL 与 TanStack Query 表达的大量跨页本地状态，再评估轻量状态库。

### 3.2 前端安全边界

- 页面导航隐藏不作为权限控制；每个 Go query/command handler 重新鉴权。
- Issue #6 角色管理（2026-08-06）：主管侧边栏新增 `/admin/roles`，消费 `GET/PUT /api/role-mappings`；Go 服务端只接受当前 `supervisor`，在 PostgreSQL 事务中锁定有效主管映射并拒绝自我停用/降级或移除最后一个有效主管。角色变化会通过既有会话逐请求 `r.role = s.role` 校验使旧角色会话失效，不依赖前端刷新。
- Issue #6 账号识别与退出（2026-08-06）：`GET /api/session` 仅向已认证本人增加 `union_id`；主管授权的角色映射 API 保持返回完整 `actor_ref`，React 账户菜单与管理列表提供复制。`POST /auth/logout` 撤销数据库会话并删除 HttpOnly Cookie，前端成功后跳转 `/login`；unionId 不进入 URL、日志或匿名恢复页面。
- Issue #6 角色视图同步（2026-08-06）：React 批次列表和导入路由从真实 `GET /api/session` 获取角色，只向运营渲染导入入口与 XLSX 表单；主管直接访问 `/batches/new` 时进入无导入权限的安全状态。该视图裁剪用于消除误导，服务端 `POST /api/batches/import` 继续逐请求校验运营角色并以 403 拒绝主管，权限真相不下放到前端。
- 产品接口只接受运营/运营主管；OA 消息使用独立 DTO 白名单，不从完整建议删除字段后发送。
- 页面错误不回显堆栈、SQL、LiteLLM 原文或目标对象身份。
- 会话 Cookie 使用 `HttpOnly`、`Secure`、`SameSite=Lax`；前端不可读角色签名或部署密钥。

> OAuth 标识与秘密边界（2026-08-05）：钉钉 `client_id` 是 OAuth 授权端识别应用所必需的公开标识，只允许出现在服务端生成、指向钉钉官方域名的授权跳转查询参数；不得进入前端静态包、业务 API 数据、业务日志或数据库业务字段。`client_secret`、机器人编码、LiteLLM Key、钉钉/会话访问令牌仍按部署秘密处理，在页面、接口、日志、错误、审计、构建产物和数据库中零明文；Docker 运行环境配置本身由运维权限保护，不提供普通用户读取入口。

### 3.3 前端风险与缓解

- 风险：服务端误把完整建议写入 OA 消息。缓解：通知专用 DTO、模板字段白名单和消息契约快照测试。
- 风险：多个页签保留旧 version 后提交覆盖。缓解：命令必须携带 version，冲突返回最新状态并保留输入。

## 4. 后端技术选型

### 4.1 推荐：Go 1.26.5 + chi 5.3.1 API + 独立 Go Worker

这是蓝图确认的直接默认。

**决策证据**：

- 导入、规则、每日 OA 催办和 AI 需要脱离 HTTP 请求生命周期，但规模不足以引入消息中间件。
- API 和 Worker 位于同一 Go Module，共享领域类型、规则纯函数、仓储接口和事务不变量，避免两套后端口径。
- chi 完全兼容标准库 `net/http`，中间件与路由层保持轻量；Go 1.26.5 是 2026-08-04 核验到的当前稳定安全修订。
- 前端/后端 DTO 通过 OpenAPI 生成类型和契约测试同步，不通过复制 Go struct 或手写 TypeScript 类型维持一致。

**进程职责**：

| 进程 | 同步职责 | 异步职责 |
| :--- | :--- | :--- |
| Go API | 登录、查询、审核、执行、结果、历史、文件接收 | 创建持久任务，不等待 AI |
| Go Worker | 领取任务、续租、幂等完成 | XLSX、指标、规则、AI 解释、每日 OA 催办、备份触发记录 |

**任务持久化**：

- PostgreSQL `job` 表保存任务类型、业务键、状态、尝试次数、可执行时间和租约。
- Worker 使用事务领取待处理任务；任务业务键唯一，重启后继续。
- 导入任务和规则任务失败不把未完成批次标为成功。
- AI 重试绑定原 `decision_id`，只追加解释版本和状态。

**当前代价**：

- 单 Worker 吞吐有限，但首轮每周 10—20 个 SPU 足够。
- 数据库任务表不适合高吞吐事件流。

**升级触发条件**：

- 连续四周任务积压超过一个导入周期，或数据库任务锁竞争成为主要延迟，再评估 Redis/消息队列。
- 需要多实例独立发布和跨服务 SLA 时再拆服务。

### 4.2 关键接口契约

| Design ID | 方法与路径 | 角色 | 关键行为 |
| :--- | :--- | :--- | :--- |
| `API-F07-01` | `GET /auth/dingtalk/start`、回调、`POST /auth/logout` | 未登录/运营/主管 | 钉钉认证、建立双角色服务端会话、恢复原安全路径 |
| `API-F01-01` | `POST /api/batches` | 运营 | 接收元数据与 XLSX，指纹幂等返回原批次 |
| `API-F01-00` | `GET /api/batches` | 运营/主管 | 20/50/100 条分页读取真实批次、处理状态与质量计数 |
| `API-F01-02` | `GET /api/batches/:id` | 运营/主管 | 批次、校验、指标和字段质量投影 |
| `API-F05-01` | `GET /api/actions` | 运营/主管 | URL 条件分页；返回批次决策、稳定任务、当前生命周期，以及最近前序原批次建议快照（主动作、关键依据、库存语境、经营状态与时间）、经营/协同状态、清仓完成确认和最近 OA 催办 |
| `API-F06-01` | `GET /api/suggestions/:id`、`POST .../review` | 运营/主管 | 双角色详情投影、整体审核、review_version 检查 |
| `API-F06-02` | `POST /api/actions/:id/execute`、`.../result` | 责任运营 | execution_version、幂等键；经营和协同轨均校验 SPU 责任运营归属 |
| `API-F06-03` | `POST /api/suggestions/:id/override`、`.../terminate` | 运营主管 | 新经营/库存动作、必填理由、review/action version；保留原规则，已执行返回 `409` 并要求先追加终止事件，再以新版本改判 |
| `API-F06-04` | `POST /api/actions/:id/clearance-completion`、`.../confirm`、`.../return` | 责任运营/主管 | 运营提交实际完成时间；主管确认或必填原因退回；version 保护；确认后闭环 |
| `API-F06-05` | `POST /api/actions/:id/oa-notifications`、`.../:notificationId/retry` | 责任运营 | 通知专用白名单、业务幂等键；手动发送/失败补发；不改变业务确认状态 |
| `API-F04-01` | `POST /api/suggestions/:id/ai-retry` | 运营/主管 | 原冻结数据重试，不改规则或状态 |
| `API-F08-01` | `GET /api/history` | 运营/主管 | 历史分页与不可变快照；按角色裁剪 |

所有接口先写入版本化 OpenAPI 3.1 契约；React 侧生成 TypeScript 类型，Go handler 通过请求/响应契约测试验证实现。所有命令响应返回当前业务版本、最新状态、操作者和时间；冲突使用明确的 `409` 语义，重复幂等请求返回既有成功结果。

> 实现同步（2026-08-04）：`API-F06-03/04` 已落地为 Go 事务命令、PostgreSQL 版本/幂等保护与 React 建议详情表单。人工改判必须更换经营动作，并在 JSON 中显式携带 `inventory_action`：`restock`、`no_restock` 或 `null`（不生成协同）三选一；漏字段、空理由、清仓改加投仍沿用禁补及旧版本均由服务端拒绝。合法改判新增唯一生效 `supervisor_override` 版本，固定规则版本只转为历史状态而不删除；任一轨已执行时必须先调用 `terminate` 追加终止事件。清仓完成由责任运营提交后保持原执行态，主管通过独立 `confirm` 或 `return` 命令确认/退回，只有确认才将经营轨关闭；退回原因和再次提交均保留版本。

> 页面实现同步（2026-08-05）：`PAGE-F06-03` 不新增聚合后端或前端占位数据，直接把现有 `SuggestionDetail` 的冻结快照、质量状态、稳定任务、动作版本、通知、清仓确认及追加事件投影为“Hero 总览→四要素→规则链→跨周续接→双轨→角色操作→时间线”。同组上一条/下一条通过带 `batch_id + action + tab=all + limit=100` 的真实行动清单查询计算，符合 MVP 单批 10—20 SPU 边界；通知失败取真实 `OANotification` 最近失败记录，仅改变警示文案，不推进业务状态。主管审核记录从真实事件中提取最近审核人和时间；数据限制由冻结 `quality` 字典显式展示，缺失值仍使用“未提供/未校验/数据不足”，不补 0。写操作继续复用版本化 Go 事务 API，页面结构调整不改变权限、幂等或状态机。

> 历史页面同步（2026-08-05）：`PAGE-F08-01` 直接消费服务端历史冻结投影，不在浏览器重放当前任务状态。批次、SPU、动作、审核、双轨执行与期间条件均由参数化历史 API 查询；筛选、页码和每页数持久化为 URL 参数。列表一行固定对应 `decision_task_link`，展示冻结规则动作、链接级状态重放、最近事件与审计异常计数；进入详情携带 `mode=history` 和安全 `return_to`，后端继续拒绝历史写入。五节点里程碑只由已发生的状态判断点亮，不把 AI/钉钉未配置自动计为契约审计异常。

> 钉钉通知实现约束（2026-08-05）：用户已确认原规格中的 OA 即钉钉。`API-F06-05` 使用服务端 `DINGTALK_CLIENT_ID`、`DINGTALK_CLIENT_SECRET` 和 `DINGTALK_ROBOT_CODE` 获取应用 Access Token，并调用企业内部机器人单聊 `POST /v1.0/robot/oToMessages/batchSend`；钉钉应用必须先在开放平台开通 `qyapi_robot_sendmsg`，否则官方接口返回 `Forbidden.AccessDenied.AccessTokenPermissionDenied`。收件人是公司钉钉 User ID，不使用 Union ID、手机号 @ 或群自定义机器人 Webhook。消息正文白名单仅含 SPU/商品链接、协同动作、责任运营、反馈要求和稳定产品内任务引用，协议外层仅含机器人编码、User ID、消息类型和消息参数。手动发送、失败补发、发送尝试次数、受控错误码和真实接口请求标识均持久化；成功响应必须保存响应正文中的 `processQueryKey` 作为 `provider_reference`，以便用官方 `GET /v1.0/robot/oToMessages/readStatus` 核对发送/已读状态，HTTP `x-acs-request-id` 仅用于请求排障且不得冒充消息查询键。接口受理只记“钉钉已受理”，查询结果也不得自动改变业务确认状态；副作用消息请求不自动重试，以免重复发送，业务幂等仍由产品通知唯一键保障。Worker 按 Asia/Shanghai 自然日创建清仓催办，确认、非清仓改判或终止后停止新提醒；责任运营显示名只有精确对应唯一非空活跃公司 User ID 时才允许发送，映射缺失记录 `dingtalk_recipient_unresolved`，同名对应多个不同 User ID 记录 `dingtalk_recipient_ambiguous`，两者均失败关闭且不得按配置时间任选收件人。完整组织同步不属于本次产品范围，不要为获取 User ID 而新增通讯录同步产品能力；只复用现有映射或由运维按审批结果配置公司 User ID。

> 催办收件人修复同步（2026-08-05）：`RunClearanceReminders` 已去除“同名映射按最近配置任选一人”的 SQL，改为在发送前计数精确显示名下的非空活跃运营 User ID；缺失或多个映射只持久化失败通知，`attempt_count=0` 且不发起钉钉 HTTP 请求。PostgreSQL 集成测试已覆盖唯一映射、缺失映射、同名歧义、业务/库存状态不变及跨日停催；FLOW-11 仍须用全新候选库的唯一真实映射完成机器人回执后才能通过。

> 真实催办验收同步（2026-08-05）：全新隔离 Compose 已从空库迁移并通过产品 API 处理真实 XLSX，在唯一真实责任运营 User ID 映射和单条清仓审批条件下，生产 Worker 只产生一条当日催办并保存有效 `processQueryKey`；官方 `readStatus` 返回 `SUCCESS`、1 个真实收件人且当时未读。同日再次运行 Worker 未重复创建或发送，且回执查询和催办发送都不改变业务/库存确认状态。

> 任务引用权限验收同步（2026-08-05）：`GET /api/suggestions/:id` 在读取前验证业务会话；匿名请求统一返回 `401 authentication_required` 且不回显对象 ID，运营查询把 `operator_ref` 与会话显示名写入同一 SQL 条件，非责任运营统一得到不暴露存在性的 `404`，主管才可读取事业部对象。真实 PostgreSQL 回归同时覆盖责任运营、非责任运营、主管和外部角色；移除运营对象过滤时该测试必须转红，防止只验证状态码而未覆盖对象裁剪。

> 历史实现同步（2026-08-04）：`API-F08-01` 已落地为独立历史投影与 React `/history`。一行严格读取一个 `decision_task_link + decision_record` 的批次/SPU 冻结建议，审核取该链接结果，双轨进度由该链接的追加事件重放，不读取当前行动清单替换历史动作；查询支持批次、SPU、经营/库存动作、审核、双轨执行状态、业务期间和 20/50/100 条分页。`GET /api/suggestions/:id?mode=history` 使用同一建议身份，返回该链接的事件及对应生效动作版本，前端隐藏全部写入口并通过 `return_to` 恢复筛选、页码和每页数量。PostgreSQL 集成验收同时覆盖组合筛选、主管改判后原规则与指标冻结、已驳回、部分执行、并发冲突、AI 失败、事件顺序和运营对象裁剪；`decision_record` 与 `spu_snapshot` 的原位规则升级或数值覆盖由不可变触发器直接拒绝，纠错只能追加版本或事件。

> 行动清单实现同步（2026-08-04）：`GET /api/actions` 与 React `/actions` 已实现待我处理/全部/处理中/已完成四页签，支持批次、SPU、经营或库存动作、店铺、责任运营、审核、经营、库存、清仓确认与任务总进度组合筛选。默认页签按运营/主管责任分别投影，筛选与页签写入 URL；历史批次入口强制追加 `mode=history` 并隐藏写操作。非法枚举筛选回到安全默认，不改变业务数据。工作台先独立解析最新 ready 批次，再在该批次内查询角色待办；运营当前没有“待我处理”行时仍显示真实最新批次号与持久化完成/创建时间，不能把“无本人待办”伪装成“无成功批次”。数据限制不从待办子集倒推，而是对当前批次 `spu_snapshot.quality` 按角色对象范围聚合字段、状态与受影响 SPU 数，页面逐项显示并提供批次详情入口。

> 行动清单状态投影同步（2026-08-05）：`ActionItem` 在版本化 OpenAPI 中新增 `latest_ai_status`、`latest_notification_status`、`latest_notification_error_code` 与 `latest_clearance_status`。Go 查询以按决策版本或任务时间倒序的标量子查询读取 PostgreSQL 最近状态，不直接连接一对多解释、通知或清仓提交表，避免把一条稳定任务扩成重复清单行；无记录保持 `null`，不得伪装成发送成功或完成。React 以这组真实字段和同一快照 `quality` 呈现 AI、钉钉、清仓确认与数据限制，钉钉接口受理仍不自动改变库存协同业务状态。

> 跨周生命周期验收同步（2026-08-04）：PostgreSQL 集成场景使用同一 SPU 的三个不同业务截止日与三份不同冻结指标。第二周同为清仓+禁补时只新增周关联，稳定任务、审核结果、双轨状态、各轨 version 和原经营执行时间均不重置；第三周规则变为观察时只追加待审核动作版本，主管确认前清仓仍为当前生效动作。最近前序指针必须精确落到第二周关联，并读取第二周原批次的销售、利润率、品退率、库存天数、触发规则和关联时经营状态，不得读取第三周数据回填。验收基数固定为 1 个稳定任务、3 条周关联、2 个动作版本。集成测试默认事务回滚；仅在显式设置 `PRESERVE_LIFECYCLE_FIXTURE=1` 且指向隔离验收库时提交同一已断言数据，供生产 API 与页面完成键盘/鼠标 E2E，不得用于 RC2 真实 XLSX 候选库。

### 4.3 核心时序

```mermaid
sequenceDiagram
    participant OP as 运营
    participant API as Go API
    participant DB as PostgreSQL
    participant WK as Worker
    participant AI as LiteLLM
    participant MG as 运营主管
    participant OA as 钉钉企业内部机器人
    participant EXT as 外部相关人员

    OP->>API: 上传 XLSX + 期间 + 截止日
    API->>DB: SEQ-F01-01 计算指纹并建立/返回批次
    API-->>OP: 返回 batch_id 与处理状态
    WK->>DB: 领取导入任务
    WK->>DB: 原子保存快照、规则和兜底
    WK->>DB: SEQ-F03-01 按 SPU 比较经营/库存两轨
    alt 两轨动作与当前生效版本相同
        DB->>DB: 关联原稳定任务，保存 linked_at 与 previous_link_id，追加续接事件
    else 任一轨动作变化
        DB->>DB: 追加待主管确认的动作版本
    end
    par 人工闭环
        MG->>API: 未执行前提交改判动作+理由+版本
        API->>DB: 校验双轨未执行，追加生效动作版本与改判事件
        MG->>API: 携 review_version 整体审核
        API->>DB: 事务更新审核并激活动作、追加事件
        OP->>API: 携 execution_version 执行经营和协同动作
        API->>OA: 使用最小字段通知外部相关人员
        OA-->>EXT: 送达协同事项
        EXT-->>OP: 通过 OA/线下反馈处理结果
        OP->>API: 记录协同结果
        alt 当前经营动作是清仓且完成时间未确认
            WK->>DB: 按任务+自然日+接收人检查幂等键
            WK->>OA: 每日提醒责任运营确认清仓完成时间
            OP->>API: 提交实际清仓完成时间
            MG->>API: 确认或填写原因退回
            API->>DB: 确认后闭环并停止后续提醒
        end
    and AI 异步
        WK->>AI: 白名单冻结数据
        AI-->>WK: 解释或错误
        WK->>DB: 追加合规解释或失败状态
    end
```

时序稳定 ID：`SEQ-F01-01` 覆盖批次幂等；`SEQ-F03-01` 覆盖规则与双动作原子生成；`SEQ-F06-01` 覆盖审核、运营统一执行与钉钉协同；`SEQ-F06-02` 覆盖清仓完成时间双人确认及每日催办；`SEQ-F04-01` 覆盖 AI 降级。

## 5. 数据层选型

### 5.1 主数据库推荐：PostgreSQL 18.4

这是蓝图已确认的直接默认。PostgreSQL 18.4 是 2026-05-14 发布的 18 系列稳定维护版本；不采用仍为 Beta 的 PostgreSQL 19。

**决策证据**：

- 批次、建议、动作和事件关系明确，需要事务、唯一约束和乐观并发。
- 清仓/止损与禁补必须原子生成，状态变化与事件必须一起成功。
- 历史按批次、SPU、动作和状态检索，关系索引足够。

**数据访问推荐**：`pgx/v5` 5.10.0 + goose 3.27.3。

- `pgx` 直接使用 PostgreSQL 连接池、事务和显式 SQL，使锁、唯一约束、`FOR UPDATE` 与追加写语义保持可审查；MVP 不引入 ORM 隐式生命周期。
- goose 管理有序 SQL 迁移；API、Worker 与测试数据库使用同一迁移链，不引入数据库代理或云专属 SDK。

**当前代价**：

- 单机 PostgreSQL 与应用同主机，不具备主从高可用。
- 运维需负责安全补丁、容量和真实恢复演练。

**升级触发条件**：

- 明确出现多实例高可用要求、单机恢复无法满足更严 RTO，或事业部扩张产生持续资源争用时，再迁移国内云托管 PostgreSQL/主备。

### 5.2 数据实体

| Design ID | 实体 | 关键约束 |
| :--- | :--- | :--- |
| `DATA-F07-01` | `user_session`、`role_mapping_snapshot` | 钉钉身份唯一；绝对/闲置到期；每请求复核角色 |
| `DATA-F01-01` | `import_batch`、`import_file`、`import_error` | 指纹唯一；期间/截止日冻结；文件摘要唯一语义 |
| `DATA-F02-01` | `spu_snapshot`、`metric_quality` | `batch_id + spu_id` 唯一；原始值、采用值、周期分离 |
| `DATA-F03-01` | `rule_version`、`decision_record` | 规则版本不可变；分类、主动作、库存动作同事务 |
| `DATA-F04-01` | `ai_explanation` | 版本化解释；状态独立；不保存密钥和越界原文 |
| `DATA-F05-01` | `spu_action_task`、`decision_task_link`、`action_revision` | `business_unit_id + spu_id` 拥有稳定任务身份；任务保存 `task_created_at/current_business_state/business_executed_at`；周度关联保存 `linked_at/previous_link_id`；前序建议通过 `previous_link_id` 关联原批次 `decision_record`、`spu_snapshot` 与当时动作/状态事件，不复制成可变字段；动作相同不创建新任务，变化版本只追加，仅一个当前生效版本 |
| `DATA-F06-01` | `action_item`、`action_result`、`effective_action_revision`、`clearance_completion`、`oa_notification` | 一个稳定任务最多有经营/协同两轨；责任运营统一执行；清仓完成时间保留提交、确认/退回版本；OA 通知以任务+自然日+接收人幂等；review/execution version 单调递增 |
| `DATA-F08-01` | `review_event`、`action_event`、`correction_event` | 追加写；含前后状态、操作者、时间、原因 |
| `DATA-F01-02` | `job` | 业务键唯一；租约、重试和最终状态可恢复 |

### 5.3 事务与不变量

- 批次指纹命中时只读返回既有对象，不创建第二清单。
- 行动 API、运营/主管工作台和行动清单页面使用同一“最新成功批次”排序：`business_cutoff_date DESC, completed_at DESC, batch_id DESC`；批次列表仍可按提交时间展示，但不得把其首行当成行动上下文，避免相同创建时间或跨周批次下标题与任务行错配。
- 规则事务先写不可变 `decision_record` 和结构化四要素，再在同一事务内按 SPU 锁定 `spu_action_task`，分别比较经营/库存动作指纹。
- 动作指纹相同时只写 `decision_task_link` 和续接事件，不新建 `action_item`、不改变 review/execution version；指纹不同时追加待主管确认的 `action_revision`。
- 写 `decision_task_link` 时，在同一事务内按 `business_unit_id + spu_id` 查询业务时间早于当前记录的最近一条关联并保存 `previous_link_id`；读取前序建议时只从该关联对应的原批次 `decision_record`、`spu_snapshot` 与截至该记录时点的动作/状态事件生成只读投影，禁止读取当前批次指标补齐历史；首次建任为空，禁止按名称或模糊文本关联。
- 经营执行成功时写 `business_executed_at`；未执行保持 `NULL`。`task_created_at`、`linked_at` 和 `business_executed_at` 不得相互回填。
- 审核事务同时更新建议 version、激活动作并追加审核事件。
- 改判事务先锁定建议并验证两轨均未执行；保留 `decision_record` 原规则动作，追加 `effective_action_revision` 和改判事件。清仓改加投时，新库存动作不得为禁补。改判只保留并递增既有审核状态/version：待审核建议改判后仍为待审核且双轨保持 `pending_review`，必须再整体通过或驳回；已通过或终止后改判保持已通过并让新动作进入待执行，不得把一次改判静默等同于整体审核。
- 执行/结果事务仅允许当前 SPU 的责任运营更新经营或协同动作 version，并追加事件；外部相关人员无产品写权限。
- 清仓完成时间提交只进入“待主管确认”；主管确认才写 `confirmed_at` 并闭环，退回必须记录原因且保留原提交版本。
- 每日催办先对责任运营显示名统计非空活跃公司 User ID：恰好一个才作为收件人，零个或多个分别持久化未解析/歧义失败且不调用钉钉。发送记录以 `action_task_id + local_date + recipient_actor_ref + template_code` 建唯一约束，其中现有数据库列 `recipient_actor_ref` 存储唯一钉钉公司 User ID或受控未解析标识；钉钉接口受理只更新通知状态，不得写入送达/已读或业务确认时间。清仓完成已确认、动作改判或任务终止时停止新建提醒。
- 纠错不修改旧快照；创建新批次或追加 correction event。

> 规则实现同步（2026-08-04）：`RULE-V1.0` 的经营动作内部枚举与全站契约统一为 `clearance/stop_loss/observe/invest/maintain`，库存动作统一为 `prohibit_restock/restock/no_restock/null`。候选审计发现新品利润率 `<-20%` 与成熟商品品退率 `>1.5%` 曾错误输出未定义值 `watch`；固定规则测试先在旧提交 `6e45b81` 复现 3 个失败分支，再改为 `observe`。全矩阵覆盖新品、淘汰品、大/小爆款、等于/内外阈值、五类经营动作、四类库存结果及字段不足；真实 PostgreSQL 另断言止损/清仓与非禁补组合为 0。

### 5.4 缓存与搜索

**推荐：MVP 不引入 Redis 和搜索引擎。**

**决策证据**：首轮 10—20 个 SPU、内部低并发、查询条件结构化。

**当前代价**：历史增长后复杂筛选依赖数据库索引和分页。

为 `GET /api/actions` 建议建立 `(business_unit_id, spu_id, linked_at desc)`、`(current_business_state, task_created_at desc)` 复合索引，分别支持最近前序记录与经营状态筛选；索引字段不改变业务身份。

**升级触发条件**：以真实慢查询和容量证据为准；优先补复合索引和归档策略，再考虑独立基础设施。

## 6. AI 能力选型

### 6.1 推荐：现有 LiteLLM 网关的结构化单轮调用

MVP 只需把已固化规则解释为对象、问题、依据和动作，不需要 RAG、Embedding、向量数据库、工具调用或自主 Agent。

**决策证据**：

- 固定规则拥有唯一的原始建议决定权；主管人工改判是独立、追加式的生效动作版本。
- 输入只有单条 SPU 的冻结白名单数据。
- AI 失败必须降级且不阻塞。
- 网关和部署密钥已由运维提供。

**调用契约**：

1. 输入：SPU 展示身份、指标实际值、周期、阈值、比较关系、最终动作和数据质量标记。
2. 输出：严格结构化的 `problem/evidence/action/summary`。
3. 校验：Go `encoding/json` 严格解码到受控 struct，并执行字段、动作、数字、粒度和敏感信息白名单检查；未知字段和不合规内容不进入展示投影。
4. 越界：舍弃不合规字段，保留固定规则兜底；日志只记非敏感追踪 ID 和受控错误码。
5. 重试：运营或主管可基于同一 `decision_id` 请求重试；旧合规版本保留到新版本成功。

> 实现同步（2026-08-05）：Go Worker 已通过 `LITELLM_BASE_URL`、`LITELLM_API_KEY`、`LITELLM_MODEL` 调用兼容 OpenAI `POST /chat/completions` 的现有 LiteLLM 网关，API 进程不接收模型密钥。每条新决策先持久化 `generating` 版本，再由 Worker 读取原冻结快照生成；重试只新增版本。响应严格限制为 `problem/evidence/action/summary` 四个非空字符串，提示词把本条固定动作组合写成唯一允许值并要求其余字段保持字符串；未知字段、动作不等于固定规则动作组合、出现输入白名单之外数字，或包含 SKU/广告明细/评价与退款归因时整段记为 `not_adopted` 且不保存原文。真实网关在 20 秒窗口出现传输超时，当前客户端超时采用 45 秒；网络/网关失败仍记受控失败码，最新失败或生成中状态继续读取上一版合规内容，不改变审核、经营或库存状态。2026-08-05 以真实 XLSX 建立 10 条冻结决策和 7 条行动建议，首轮 6 条越界、4 条传输失败均未采纳；修正提示约束和超时后通过正式重试 API 追加版本，最终 7/7 行动建议最新解释为 `generated`，固定决策与任务摘要前后一致。

> AI 展示实现同步（2026-08-05）：API 继续返回并持久化原有四字段解释对象，不改变版本、状态或校验语义。React 展示层把四字段渲染为中文标签卡片，并对字段内已通过服务端校验的嵌套 JSON 字符串做只读解码及动作枚举中文化，避免生产页面暴露原始 JSON 和英文内部枚举；格式化结果不回写数据库，不参与规则、审核或双轨状态计算。

**当前代价**：不支持跨 SPU 预算优化、知识检索、自动工具执行或多步推理。

**升级触发条件**：只有新的稳定 REQ 明确需要跨文档检索、工具调用或自主多步任务，才分别评估 RAG 或 Agent 框架。

### 6.2 明确不引入

- 不引入前端或 Node AI SDK：Go 标准 HTTP Client 直连现有 LiteLLM REST 网关已足够，增加编排层不能改变网关责任。
- 不引入 LangChain/LangGraph/Mastra：没有多步工具调用需求。
- 不引入 pgvector/Milvus/Qdrant：没有知识库检索需求。
- 不在方案中指定模型名称、网关地址、密钥变量名、超时和成本阈值；这些由运维环境与实施验证确定。

### 6.3 AI 风险与缓解

- 虚构数值：输出数字必须存在于输入白名单，否则丢弃。
- 动作冲突：推荐动作从规则字段渲染，不采信模型动作文本。
- 敏感泄露：不发送人员标识、完整 XLSX、日志、密钥；发给外部相关人员的 OA 消息只含 SPU 身份、协同动作、截止时间和责任运营联系方式等白名单字段。
- 网关失败：结构化四要素先就绪，AI 状态独立，业务继续。

## 7. 基础设施选型

### 7.1 部署与托管

**推荐：公司内网或国内云单台持久化 Linux 主机 + Docker Compose 5.1.2。**

**决策证据**：低并发内部工具、单实例已确认、需要常驻 Worker、数据库和 XLSX 持久化、国内可达。

**服务组成**：Nginx（托管 React 静态产物并反代同源 `/api` 与 `/auth`）、Go API、Go Worker、PostgreSQL 四个服务，以及由 API/Worker 调用的钉钉开放平台；数据库和 XLSX 使用显式持久卷/目录。Node 只存在于前端构建/测试阶段，不进入生产运行镜像。

**当前代价**：主机是单点；升级和回滚由运维窗口完成。

**升级触发条件**：业务明确要求多实例、无停机发布或更严可用性，再比较国内容器服务；MVP 不引入 Kubernetes。

### 7.2 认证与授权

**推荐：钉钉统一身份 + Go 服务端会话 + PostgreSQL 角色映射快照。**

**决策证据**：钉钉为唯一入口；事业部负责人审批，运维/系统管理员配置；无映射默认拒绝。

**当前代价**：接入协议、应用配置和稳定 actor_ref 需实施验证。

**升级触发条件**：只有公司统一 IDaaS 替换钉钉或新增稳定需求时，才替换认证适配器；业务授权模型保持不变。

### 7.3 文件存储

**推荐：服务器持久化目录 + PostgreSQL 文件元数据。**

| 判断事实 | 当前结论 |
| :--- | :--- |
| 实例 | 单实例 |
| 磁盘 | 持久化 Linux 磁盘 |
| 文件规模 | 每周 XLSX，首轮 10—20 SPU；Nginx 与 Go multipart 统一限制 32 MiB，避免默认 1 MiB 阻断真实文件或无界上传 |
| 直传/CDN | 不需要 |
| 跨实例共享 | 不需要 |
| 备份 | 每晚异机，RPO≤24h、RTO≤4h |

当前不引入 OSS/COS/MinIO。多实例共享、临时磁盘、文件量显著增长或客户端直传/CDN任一真实出现时，再做增量选型。

### 7.4 可观测性

**推荐：结构化 JSON 应用日志 + Nginx 访问日志 + Docker 健康检查 + PostgreSQL 业务审计。**

不引入海外 Sentry SaaS、PostHog Cloud 或新付费监控平台。若公司已有主机/日志平台，由运维接入标准输出，不改变应用契约。

日志保留 90 天，不记录部署密钥、完整 XLSX、LiteLLM 原始请求或被过滤内容。建议指标包括导入成功率、字段降级数、规则处理时长、AI 成功率、审核冲突数、待确认清仓完成数、OA 发送成功率、同日重复提醒拦截数、任务积压、备份结果与恢复演练结果。

> 实现同步（2026-08-04）：已认证用户对真实任务发起无权命令或旧版本命令时，HTTP 分别返回 403/409，同时在独立事务追加 `authorization_denied` / `version_conflict` 事件；审计写入不改变任务状态，且审计失败不得遮蔽原业务响应。普通错误严格返回 OpenAPI 约定的字符串 `error`；经当前对象权限校验后的 409 另返回 `latest`，含审核/经营/协同最新状态与版本、最后操作者和时间，React 保留表单输入并直接展示该信息。历史投影只计数并按权限展示这些异常事件。

**当前代价**：没有集中式调用链和错误平台。

**升级触发条件**：排障耗时或多实例上线形成真实需求时，再接公司现有可观测平台或自托管 OpenTelemetry/Grafana；不得静默新增外部服务。

### 7.5 备份与恢复

- 每晚备份 PostgreSQL、原始 XLSX、文件元数据和配置清单到公司受控异机存储。
- 保留 30 个日备份和 12 个每月归档。
- 每次备份产生非敏感清单、校验和、开始/结束时间和状态。
- 每季度在隔离环境真实恢复数据库和文件，核验批次、建议、事件和文件关联。
- 恢复演练耗时超过 4 小时或最新可恢复点超过 24 小时即判定目标未达成。

> 实现同步（2026-08-04）：`deploy/backup.sh` 以 Compose 项目为边界，将 PostgreSQL data-only custom dump、XLSX 持久卷压缩包、代码/迁移版本等非敏感 manifest 与 SHA256SUMS 写入同一新快照目录。`deploy/restore.sh` 先校验摘要，再要求目标迁移已完成且 `import_batch` 等业务数据与 XLSX 卷均为空，然后恢复数据和文件；不提供清空目标、覆盖文件或 `pg_restore --clean` 能力。异机目录与每日调度由运维提供，脚本不自行引入对象存储或调度系统。

### 7.6 CI/CD

**推荐：GitHub Actions 在代码与测试命令建立后分别运行 React 的 lint/typecheck/Vitest/build、Go 的 `gofmt`/`go vet`/`go test -race`/build、OpenAPI 跨端契约校验、集成/E2E 与镜像构建；部署由运维受控执行。**

> 实现同步（2026-08-04）：仓库已具备稳定代码与测试入口，`.github/workflows/生产门禁.yml` 在 push 和 pull request 上执行锁文件安装、OpenAPI TypeScript DTO 生成无差异、React lint/typecheck/Vitest/build、空 PostgreSQL 迁移、Go `gofmt`/`vet`/`test -race`/build，并构建、启动隔离 Compose 项目后验证健康探针与登录页。CI 使用测试专用 PostgreSQL 和服务默认拒绝外部集成的模式，不注入、伪造或轮换钉钉/OA/LiteLLM 密钥，不执行 deploy。根 `.dockerignore` 排除 Git 元数据、依赖/构建缓存、环境文件和全部 XLSX，真实经营工作簿不进入 Docker 构建上下文或镜像。

> 验收边界同步（2026-08-05）：产品候选验证部署密钥不进入前端包、页面、接口、日志、错误、审计或业务数据库，并验证未配置/失效时默认拒绝或受控降级；钉钉/LiteLLM 平台侧密钥轮换、撤销旧值和更新外部环境仍由运维负责，不由产品验收自动执行，也不作为本次去扫码登录增量收口的重复外部操作。

## 8. 实施建议与风险全景

### 8.1 推荐技术栈一览（最终决断）

| 层级 | 推荐技术 | 版本 / 备注 |
| :--- | :--- | :--- |
| 前端构建 | Vite | 8.2.0，`npm view` 核验；Node 仅用于构建/测试 |
| UI | React + React DOM | 19.2.8，`npm view` 核验 |
| 前端语言 | TypeScript | 5.9.3；7.0.2 与 typescript-eslint 8.57.0 的 peer range 冲突，已按脚手架兼容性烟测结果回退 |
| 前端路由 | Browser History/Location | 页面集合仅需静态客户端分发；React Router 7.x 可用版本在本轮安全审计中均命中高危通告，因此移除该非必要攻击面 |
| 前端服务端数据 | TanStack Query | 5.101.4，与 OpenAPI 生成 DTO 组合管理请求、缓存失效和候选界面状态 |
| 跨端契约 | OpenAPI 3.1 + openapi-typescript | 7.13.0，`npm view` 核验 |
| 后端运行时 | Go | 1.26.5，Go 官方发布记录与本机 `go version` 核验 |
| HTTP 路由 | chi | 5.3.1，`go list -m ...@latest` 核验 |
| 数据库 | PostgreSQL | 18.4，官方发布说明核验 |
| 数据访问/迁移 | pgx + goose | 5.10.0 / 3.27.3，`go list -m ...@latest` 核验 |
| XLSX | Excelize | 2.11.0，`go list -m ...@latest` 核验 |
| 后端校验 | Go struct + validator | 标准库严格 JSON 解码 + validator 10.30.3 |
| AI | 现有 LiteLLM 网关 | 版本、模型和地址由运维配置；2026-08-05 已完成真实连通、失败降级与合规重试验证 |
| 部署 | Docker Compose | 5.1.2，Docker 官方安装文档核验 |
| 文件 | Linux 持久化目录 | 与数据库统一异机备份 |

版本核验日期均为 2026-08-04。正式锁文件生成前先做 Vite/React 与 TypeScript 7 的脚手架兼容性烟测；若任一前端依赖的 peer range 未接纳 TypeScript 7，则在不改变 React + TypeScript 架构的前提下锁定其最新兼容稳定版并记录证据。Go 依赖必须写入 `go.mod/go.sum`，前端依赖必须写入唯一锁文件。

### 8.2 第一周脚手架建议（MVP 范围）

> 实现同步（2026-08-05）：仓库已建立 `web/`、`backend/`、`deploy/`、根 `compose.yaml` 与 `Makefile`。首个纵向切片使用 PostgreSQL `role_mapping/user_session`、真实钉钉 OAuth 服务端适配器、HttpOnly 会话及 React `/login`、`/auth/recovery`；缺少部署密钥时保持默认拒绝，不提供本地账号或认证绕过。钉钉适配器使用生产官方端点，HTTP 契约测试只注入隔离测试端点，验证授权参数、授权码换 token、token 换稳定 unionId 及各类非 2xx/缺字段/非法 JSON 默认拒绝。MVP 普通浏览器登录页只保留一个指向 `/auth/dingtalk/start` 的点击按钮；不生成扫码，不实现或验收钉钉客户端/工作台入口。会话生命周期由真实 PostgreSQL 集成测试覆盖运营/主管唯一角色、无角色、角色变化、角色停用、闲置 2h、绝对 12h、撤销；每次认证重新对照当前角色映射。Compose 内数据库使用 `5432`，默认映射本机 `55432`以支持 API/Worker 独立启动与真实持久化冒烟；PostgreSQL 18 持久卷按官方镜像的主版本目录结构挂载到 `/var/lib/postgresql`。工程入口以根 README 与 Makefile 为准，后续闭环继续沿用同一目录和事务边界。

> OAuth 回调诊断同步（2026-08-05）：生产回调把一次认证失败按 `state_cookie`、`state_validation`、`token_exchange`、`current_user`、`role_mapping` 五个受控阶段写入结构化应用日志，用户界面继续只显示通用认证失败或无角色恢复状态。日志不得写入授权码、OAuth state、访问令牌、Client Secret、unionId 或其他个人身份标识；该阶段码仅用于区分钉钉协议、浏览器状态与站内映射故障，不改变默认拒绝和角色校验。

> 同源路由约束：Nginx 与 Vite 仅将 `/auth/dingtalk/*` 和 `/auth/logout` 代理到 Go API；`/auth/recovery` 是 React 页面路由，必须由 SPA fallback 提供，禁止使用宽泛 `/auth/` 代理将其截获。

> 验收实现同步（2026-08-04）：XLSX 解析与批次集成测试在提供 `TEST_XLSX_PATH` 时读取该真实文件；本轮正式候选使用仓库根目录用户提供的 `商品链接.xlsx`。测试未配置外部文件时由 Excelize 生成包含 10 条中文经营数据的真实 `.xlsx` 以避免跳过解析器，但该夹具不替代正式验收输入。批次领域服务自身再次校验玩具事业部与完整自然月，不能依赖 HTTP 层门禁；同指纹使用事务级 advisory lock 保证并发只创建一批。汇总行不进入快照；当全部明细月净销售额可比较且源表汇总差异超过 0.01 元时追加 `summary_net_sales_mismatch` 警告，不回写或分摊明细。各 PostgreSQL 集成测试自行建立所需角色与领域记录，不依赖其他测试包的执行顺序。

> 全量测试隔离同步（2026-08-04）：`action`、`batch`、`explanation`、`identity` 四个使用同一 `TEST_DATABASE_URL` 的 PostgreSQL 集成测试包，在包级 `TestMain` 通过专用连接持有数据库 advisory lock，仅序列化跨包 fixture；包内并发、事务与 `-race` 仍照常执行。批次真实 XLSX 用例完成后将其专属验收批次标记为非 `ready`，保留不可变快照和审计产物，同时不污染后续“最新成功批次”查询。锁获取和释放均有明确超时，任一步失败即让测试红灯，不静默跳过真实数据库验证。

1. 在单仓建立 `web/` React + TypeScript + Vite 和 `backend/` Go Module，定义统一开发、测试与构建入口。
2. 建立 OpenAPI 3.1 基线、React 生成 DTO 与 Go 请求/响应契约测试，先锁定登录、批次和行动查询最小接口。
3. 用 goose 建立 PostgreSQL 迁移和测试数据库，用 pgx 实现事务仓储骨架。
4. 实现 Go 会话、角色映射、对象授权和钉钉回调；React 建立登录、恢复与受保护路由。
5. 实现 Go 批次上传、文件摘要、持久化目录、Excelize 解析、字段校验和 SPU 快照。
6. 在共享 Go 领域包中以纯函数实现规则版本、分类、主动作和原子库存动作，并接入数据库任务表与 Go Worker。
7. 实现行动清单/详情的 Go 查询投影和 React 页面，再实现审核、动作、结果 version 与追加事件。
8. 实现 OA 白名单、清仓完成确认/催办与 LiteLLM 结构化兜底，覆盖失败降级。
9. 建立 Nginx 同源路由、Docker Compose、备份恢复脚本和 React/Go/OpenAPI/集成/E2E 测试门禁。

### 8.3 后续阶段扩展预留

| 扩展点 | 触发能力 | 预留方式 |
| :--- | :--- | :--- |
| 数据源适配器 | 新 API/模板 | 保留 `source_type/source_version`，不实现连接器 |
| 证据类型 | 评价、退款原因、广告明细 | 版本化 evidence union，不在 MVP 建表堆空字段 |
| 动作回执 | 外部系统自动写回 | `external_receipt_ref` 为空，不触发外部调用 |
| 决策对象 | SKU | 未来作为 SPU 下级对象，不改变历史 SPU 主键与快照 |

这些候选没有稳定后续需求 ID。正式建设前必须回到 PRD 形成 REQ/AC，再做增量设计。

### 8.4 全局风险与缓解

| 风险 | 影响 | 缓解措施 |
| :--- | :--- | :--- |
| 钉钉接入或角色映射不可用 | 真实经营数据无法安全开放 | 上线前使用运营、运营主管和无角色账号验证；失败默认拒绝 |
| OA 发送失败或重复催办 | 协同人未收到事项，或责任运营被重复打扰 | 独立失败状态与手动补发；任务+自然日+接收人唯一键；业务确认与送达状态分离 |
| 清仓完成时间长期未确认 | 清仓任务无法真实闭环 | 责任运营提交、主管确认/退回；确认前每日提醒；动作改判或任务终止立即停催 |
| 数据字段周期不可信 | 观察、加投或补货误判 | 字段级降级、周期显式展示、缺失不补 0 |
| 单机和本地文件故障 | 批次、事件、XLSX 同时丢失 | 每晚异机备份、校验和、季度真实恢复演练 |
| LiteLLM 越界或失败 | 解释误导或不可用 | 白名单、结构校验、动作字段只读、结构化兜底 |
| 并发与网络重试 | 重复审核或后写覆盖 | 指纹、业务唯一键、幂等键、version 和追加事件 |
| Go/TypeScript 跨端契约漂移 | 页面误读空值、枚举或版本并提交错误命令 | OpenAPI 单一契约、生成 TypeScript DTO、Go 契约测试与 CI diff 门禁 |

### 8.5 技术负责人的最终结论

#### 是否可以立即开工

- 明确选择：有条件开工。
- 条件：运维提供可验证的钉钉应用 Client ID/Secret、企业内部机器人编码、公司 User ID 映射、LiteLLM 产品环境连接、持久化 Linux 主机和公司受控异机备份目标。

#### 最大的技术不确定性

> 不是规则算法，而是实施环境的钉钉身份/User ID 映射、企业内部机器人接口受理与实际收件、LiteLLM 连通性和真实恢复能力；这些能力必须以测试证据验收，不能只凭配置声明。

#### 现在最不应该做的事

> 不要引入微服务、Redis、搜索引擎、对象存储、Agent 框架或 Kubernetes，也不要在缺少周期和字段时用 AI 补数据。它们不能解决当前跨部门利润与库存断点，只会扩大首版故障面。

### 8.6 版本核验来源

- Go Releases：`https://go.dev/doc/devel/release`
- React Versions：`https://react.dev/versions`
- Vite Guide / Releases：`https://vite.dev/guide/`、`https://vite.dev/blog`
- chi Releases：`https://github.com/go-chi/chi/releases`
- PostgreSQL 18.4 Release Notes：`https://www.postgresql.org/docs/release/18.4/`
- Docker Compose install：`https://docs.docker.com/compose/install/linux/`
- npm 包版本：2026-08-04 使用 `npm view <package> version` 实测；Go 依赖使用 `GOPROXY=https://goproxy.cn,direct go list -m -json <module>@latest` 实测。
