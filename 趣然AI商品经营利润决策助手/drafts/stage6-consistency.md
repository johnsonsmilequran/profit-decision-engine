# 阶段 6 正式版生成与一致性报告

## 结论

`output/ppt.md` 和 `output/ppt/p01.html`—`p11.html` 已依据 `output/PRD详细版.md` revision 11 重新生成，完整纳入 F09 唯一 Admin 管理增量。11 页 HTML 逐页自检与全局检查通过。

本轮没有重新生成 PPTX；仓库内既有两个 PPTX 仅保留为 F01—F08 历史版，不得当作 F09 正式汇报件。当前正式汇报入口为 `output/ppt/p01.html`。

## F09 关键结论下推

| 正式 PRD 结论 | 阶段 6 落点 | 结果 |
| :--- | :--- | :--- |
| F09 是 F01—F08 的价值解锁条件，不单独认领全部经营收益 | P02、P04 区分解锁价值与待验证的 72/216 万元假设 | PASS |
| 无唯一角色不再停在通用错误页 | P06 展示钉钉认证到正确工作台的六段流程和五态 | PASS |
| 单 Admin 不使用共享密码或第二账号规避单点 | P07 展示两级恢复、受控交接、旧会话撤销与非静默通知 | PASS |
| Admin 与业务身份域分离，授权事实原子变更 | P09 展示双身份域、写入安全门、五项授权事实与并发护栏 | PASS |
| 产品范围、生产门禁和后续候选必须分层 | P10 以三阶段路线图呈现，不预埋通用 IAM | PASS |
| 代码完成不等于第一版完成 | P11 将产品、安全、时效和上线门禁同时设为完成条件 | PASS |

## HTML 验证证据

- 产物数：11 个 HTML，页号 01—11 连续。
- 每页均包含共享 `:root`、1920px `.stage`、`innerWidth / 1920` 缩放和左右键导航。
- 所有前后页链接连续；首页与尾页边界正确。
- `:root` 外硬编码 hex 为 0；无 `position: fixed`、hover、animation、transition 或局部纵向滚动。
- 逐页渲染检查未发现 1920×1080 溢出、裁切、遮挡或异常换行；每页可见文字不超过 300 字符。
- `git diff --check` 通过。

### 全局终检补充证据（P01—P11）

- **首次结论：视觉渲染通过，发现 3 类可机械修复的严格 HTML 契约偏差。** 下列为修复前证据。
- **共享视觉契约：** 11 页的共享颜色、阴影和圆角变量值一致；P01—P03、P05—P07、P09、P11 的 `:root` 与 `task.md` 原块一致。P04、P08、P10 在同一 `:root` 内额外声明 `--panel`、`--rule`、`--accent-strong`、`--font` 别名，虽未改变共享变量值，但不满足“原样粘贴共享 `:root` 块”的严格要求。
- **导航连续性：** 页码 01—11 连续；P02—P10 的上一页/下一页链接及左右键目标均连续，P01 左键停留首页、P11 右键停留尾页，边界无断链。
- **画布与缩放：** 11 页均有 `width: 1920px`、按 `innerWidth / 1920` 等比缩放和 `body.height = stage.offsetHeight × s`。P01—P09、P11 含 `min-height: 1080px`；P10 只有 `height: 1080px`，缺少规范要求的 `min-height: 1080px`。P01—P06、P08—P09、P11 使用规定的 `fit()` 模板；P07、P10 为功能等价改写，不满足“禁止自由改写”的逐字模板要求。
- **静态安全检查：** 11 页 `:root` 外硬编码 hex 为 0；`position: fixed`、`:hover`、`animation`、`transition`、局部 `overflow-y: scroll/auto` 均为 0。
- **重点真实渲染：** 使用 Chrome 以 1920×1080、device scale factor 1 分别渲染 P04、P08、P10，三页输出均为 1920×1080 PNG；逐张原尺寸查看，未发现溢出、裁切、遮挡或异常换行。P10 的 `min-height` 缺失目前未造成该固定画幅下的视觉故障，但仍是契约偏差。
- **最终处置：CLOSED。** P04/P08/P10 的额外别名已移出共享 `:root`，P10 已补 `min-height: 1080px`，P07/P10 已恢复规定 `fit()` 模板；重跑 11 页静态全局检查和 `git diff --check` 通过。P04/P08/P10 修改只移动 CSS 变量作用域/补等价画布约束，此前 1920×1080 实渲结论不受影响。

## 范围边界

- 72 万元/年、216 万元/三年只是用户确认的待验证价值假设，不是已节省。
- 不把 AI 设为固定规则裁决者。
- 不扩展通用 IAM、OA 审批引擎、批量授权、高级审计导出、评价/退款原因、广告明细、外部自动写回或 SKU 级能力。
