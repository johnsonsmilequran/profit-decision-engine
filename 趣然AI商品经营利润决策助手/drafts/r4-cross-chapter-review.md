# r4-cross-chapter-review.md · 跨章节平滑评审

## 总分

- C1—C6 六类冲突扫描完成。
- 发现 11 处需要修复：5 个 P0、6 个 P1。
- C1 数字/数量与 C6 量纲/单位未发现直接冲突：F09 的 1 名 Admin、2 个业务角色、2 个可靠属性、5 个持久状态、6 项 MVP 范围、7 个里程碑、10 分钟、30 天、12 次/年、72 万元/年和三年 216 万元在有效新增章节中一致；保存型恢复码也统一为一枚。
- 当前不建议交付。Controller 必须先修复全部 P0，并同步处理失效引用；P1 数量超过 5 个，修订后应做一次最小 R4′ 回归。

---

## 🔴 P0 必须修复（同一概念口径直接矛盾）

### C3.1 合并后章节号未回填，F09 完成门禁与计时 AC 指向错误章节

- `output/PRD详细版.md:53` 写：“首次启用的 10 分钟结果只有在 **§0.2** 全部门禁满足后才有效。”但 F09 完整门禁实际位于 `:55` 的“### **0.5 F09 第一版完整上线门禁**”；§0.2 是 F01—F08 的旧前置能力表。
- `output/PRD详细版.md:75` 再写：“生产门禁：**§0.2 的十项检查**”，十项检查实际为 §0.5 `:59-70`。
- `output/PRD详细版.md:2126` 写：“仅在 **§0.2** 全部门禁满足后有效 | **§8.3** 固定七个服务端里程碑”，而七个里程碑实际位于 §8.5 `:2138-2148`。
- `output/PRD详细版.md:2150` 与 AC-F09-15 `:2722` 同时继续引用 §0.2；AC-F09-15 `:2722` 还引用不存在于该内容的 §8.3。
- `output/PRD详细版.md:1616` 与 NFR-010 映射 `:2934` 引用“**§9.G**”；合并后的实际标题是 `:2385`“#### **9.7.G 外部依赖与上线门禁**”。
- **影响**：实现、验收和上线签署会读取旧表或不存在的章节，导致完整生产门禁与七个里程碑被错误跳过。
- **建议**：统一修为 §0.5、§8.5、§9.7.G；再全文扫描所有 `§X` 引用，确保目标标题真实存在。AC-F09-15 的门禁事实仍以 §0.5 为唯一总口径。

### C4.1 “阶段 2—4 未执行”与 F09 已完成阶段 2—4 的决策状态同时存在

- `output/PRD详细版.md:5` 写：“阶段 2—4 未执行”；`:101` 写：“阶段 2 的价值论证未执行”；`:2790`、`:2806-2808`、`:2890` 继续把阶段 2—4 标为未执行或跳过。
- 同一 PRD `:144` 标题为“价值论证（阶段 2 已确认）”，`:150` 给出 72 万元/年和三年 216 万元的已确认论证边界；`:2820-2823` 明确引用 `proposal-v1.md`、辩论和“用户阶段 2 价值确认”。
- `output/PRD详细版.md:2851` 虽说明“阶段 2—4 未执行”只是旧写作时点的过时描述，但没有移除前述仍具规范语气的相反结论。
- **影响**：§1、§11、§12 对同一项目阶段事实给出相反决定，可能导致 Controller 删除 F09 价值依据、竞品依据或 V1 决策。
- **建议**：所有旧表述明确限定为“F01—F08 原始 V0 当时未执行”；在总述中写清“F09 已完成阶段 2 价值确认、阶段 3 调研、阶段 4 V1 与 MVP 拍板”。不得继续使用“本 PRD 阶段 2—4 未执行”的全局句式。

### C4.2 F07 旧无角色结果/配置责任与 F09 新闭环给出两个同时有效的答案

- `output/PRD详细版.md:192` 写：“运维/系统管理员按审批结果配置并维护……产品不提供……业务侧角色管理后台；无有效映射时默认拒绝进入业务页。”FR-7 `:1246`、`:1253`、`:1265` 和 AC-F07-06 `:2674` 仍规定“运维/系统管理员配置；无角色则拒绝并联系运维”。
- F09 角色结论 `output/PRD详细版.md:246-249`、`:256` 明确“事业部负责人审批，唯一 Admin 核验并执行配置，运维不决定业务角色”；FR-9.4 `:1454-1496` 与 AC-F09-07 `:2714` 要求无角色用户进入有人承接的待处理闭环。
- `output/PRD详细版.md:2005` 又概括“无角色用户默认拒绝”，与同章 `:2033-2044` 的“同一待处理生命周期”相反；§13.2 `:2984` 仍把“运维/系统管理员配置、无映射默认拒绝”标为 PASS。
- **影响**：同一钉钉认证成功、无唯一角色的触发条件既可能落到旧错误页，也可能生成待处理；角色变更执行人也可能被实现为运维或 Admin 两套路径。
- **建议**：保留 AC-F07-05 的已有唯一角色正向行为；将 AC-F07-06 明确拆为“认证失败/登录态失效仍拒绝”和“认证成功但无唯一角色由 AC-F09-07/08 接管”。所有现行责任表统一为事业部负责人审批、Admin 配置，运维只负责身份接入与恢复部署；历史基线若保留必须标“F09 上线前”。同步修正 §13.2 的 PASS 行。

### C5.1 “审批材料可用时间”既是服务端确认时刻，又是 Admin 可回填的实际取得时间

- 数据模型 `output/PRD详细版.md:730` 把指标起点定义为 `configuration_started_at`：“Admin 确认材料已具备并开始本次配置的**服务端事件**”；`:818`、`:863` 延续同一口径。
- 界面字段 `output/PRD详细版.md:1518` 却要求“材料可用于本次配置时间”为必填时间，并写成“由 Admin 明确确认其**取得有效材料的时间**与来源”，允许它成为早于当前操作的人工事实。
- 成功指标 `output/PRD详细版.md:2134` 又使用“Admin 明确确认……的**服务端时间**至角色配置成功”。
- **影响**：同一“日常产品内配置时间”的起点可能是材料实际送达时刻，也可能是 Admin 打开配置并确认的服务端时刻；两者差值正是材料传递等待，30 天统计不可比较。
- **建议**：按 R3 PM/架构统一采用服务端 `configuration_started_at` 作为产品内配置时间起点，页面只提供“确认材料已具备并开始配置”动作而不是可任意回填的时间输入。若组织仍要记录外部实际取得时间，另作为来源事实展示，不进入该指标公式，也不新增“已审批待配置”状态。

### C5.2 “完整授权周期”被声明覆盖停用，但终点要求进入正确工作台

- 数据模型 `output/PRD详细版.md:806` 写：“首次授权、变更、停用、恢复各自关联”授权周期；FR-9.7 `:1619` 也要求四类操作都关联“下一次正确角色工作台访问”。
- 指标 `output/PRD详细版.md:2135` 同时写：“每次新增、变更、停用或恢复使用独立授权周期；待处理身份产生至下一次正确角色业务会话进入工作台。”
- 但停用行为在 FR-9.5 `output/PRD详细版.md:1533` 的唯一结果是“用户进入明确停用页”，五态 `:1915-1918` 也不允许停用后直接进入工作台。
- 术语表 `output/PRD详细版.md:2911` 又只把完整授权周期定义为“待处理身份首次产生到正确角色业务会话访问工作台”，无法描述已有用户的变更、停用或恢复周期起点。
- **影响**：停用周期永远缺少终点；已有用户的变更/恢复也没有“待处理身份首次产生”这一新起点，指标会被永久标为无法形成或被错误补时间。
- **建议**：保留 `AuthorizationCycle` 作为四类操作的通用关联实体，但把“完整授权周期”指标限定为需要重新进入工作台的首次授权、变更、恢复；停用周期终点定义为停用事务成功/停用状态生效，作为独立操作周期，不纳入“进入工作台”指标。分别明确每类周期起点，禁止用永久身份的首次待处理时间替代。

## 🟠 P1 建议修复（口径模糊或追溯不完整）

### C2.1 部署恢复的通知状态存在五套近义名称，缺少机器状态与显示文案映射

- 数据模型 `output/PRD详细版.md:832` 使用“待投递、失败待补偿、已闭环”。
- FR-9.3 状态表 `output/PRD详细版.md:1428` 使用“待投递、已投递、失败待补偿、补偿已闭环”。
- 流程 `output/PRD详细版.md:1889-1894` 使用“通知待处理、处置已闭环、持续告警并重试”。
- AC-F09-06 `output/PRD详细版.md:2736` 使用“安全通知处置失败待处理、已闭环”；§9.7.G `:2399` 使用“通知失败待补偿”。
- **建议**：统一机器状态，例如 `pending_delivery / delivered / failed_pending_compensation / compensated_closed`，并给出唯一中文显示文案；“投递成功闭环”和“补偿闭环”可共享总闭环结果，但不得把 `已投递` 与 `补偿已闭环` 混成同一原始状态。

### C3.2 F09 用户故事引用不存在的 `FR-9`

- `output/PRD详细版.md:272`、`:276`、`:280` 三个 F09 用户故事均写“（由 **FR-9（F09）** 实现）”。
- §5 实际只有 `FR-9.1` 至 `FR-9.7`，从 `output/PRD详细版.md:1326` 到 `:1593`；追溯矩阵 `:2940-2946` 也将各 REQ 映射到这些细分功能。
- **建议**：按 §12.7 的实际映射，把 US-F09-01 引用 FR-9.1/9.2/9.3/9.6/9.7，US-F09-02 引用 FR-9.5/9.6/9.7，US-F09-03 引用 FR-9.4/9.7；或明确声明 `FR-9` 是可解析的父功能并在 §5 建立真实标题。不要引用不存在的功能节点。

### C3.3 §10.0 验收索引遗漏全部 F09 AC

- `output/PRD详细版.md:2407-2459` 的“验收索引”只列 AC-F01—F08，最后一行是 AC-F06-12。
- F09 的 15 条 AC 实际存在于 `output/PRD详细版.md:2702-2770`，且注册表已将其标为 active / MVP。
- **建议**：在 §10.0 追加 AC-F09-01—15，不创造旧式 `AC-N` 展示别名；直接使用稳定 ID、优先级与对应章节锚点即可。

### C3.4 NFR-008/009 引用了不承载相应结果的 AC-F09-15

- NFR 映射 `output/PRD详细版.md:2778` 把 AC-F09-15 作为“不变管理审计”的承载 AC，`:2779` 又把它作为“授权正确性与撤销及时性”的承载 AC。
- AC-F09-15 `output/PRD详细版.md:2722` 只定义门禁满足后的七个里程碑和 ≤10 分钟结果，没有审计覆盖、错误授权或旧会话撤销结果。
- **建议**：NFR-008 由 AC-F09-09、11、12、14 承载；NFR-009 由 AC-F09-06、09—13 承载。若要让 AC-F09-15 承载三项安全指标，必须把对应可观察结果写回该 AC；当前 R3 已刻意将其收敛为计时 AC，优先移除错误引用。

### C3.5 §13 自检仍只覆盖 F01—F08，却把过期身份口径标为 PASS

- `output/PRD详细版.md:2960-2972` 的用户故事到功能映射只到 US-F08-01，没有 US-F09-01—03。
- `output/PRD详细版.md:2974-2985` 的跨章节口径没有单 Admin、双身份域、五态、恢复双结果或七里程碑；其中 `:2984` 仍把“运维/系统管理员配置、无映射默认拒绝”标为 PASS，与 F09 冲突。
- **建议**：扩展 §13 自检至 F09，引用 §12.7 的完整追溯矩阵，并在身份行使用“事业部负责人审批、Admin 配置；认证成功无角色进入待处理；认证失败/会话失效拒绝”的统一口径。

### C4.3 技术栈状态同时写成“已确认”和“不可虚构”

- `output/PRD详细版.md:2263` 写：“以下实现边界已在设计与 TDD 之后由用户重新确认”，`:2267-2269` 明确后端 Go、前端 React + TypeScript、数据库 PostgreSQL。
- `output/PRD详细版.md:2893` 又写：“LiteLLM API 与运维文档当前未提供；不虚构网关地址、认证协议、模型或**技术栈**。”
- **建议**：把 `:2893` 限定为“不虚构 LiteLLM 的网关地址、认证协议和模型参数”；删除“技术栈”，避免否定 §9.F 已确认的 Go/React/PostgreSQL 决策。

## 六类扫描结果

| 类别 | 结果 | 说明 |
| :--- | :--- | :--- |
| C1 数字/数量 | PASS | 未发现 F09 数量、阈值、金额或时限的直接冲突；216 万元被正确限定为三年假设成立时的价值上限 |
| C2 术语/命名 | P1 | 通知状态名称未统一；五个业务身份状态本身一致 |
| C3 引用/链接 | P0 | 合并后章节号错误；另有 US→FR、AC 索引、NFR→AC 与自检遗漏 |
| C4 决策一致性 | P0 | 阶段状态和 F07→F09 角色配置/无角色行为未完成增量替换 |
| C5 时间口径 | P0 | 审批材料可用起点和停用授权周期终点不唯一 |
| C6 量纲/单位 | PASS | 金额均按人民币语境，时长使用分钟/天/年且换算无矛盾；无 USD/RMB、件/pcs 等混用 |

## 整体判断

- ❌ 当前不建议进入阶段 5.5 终审或发布。
- Controller 应先修复 5 个 P0：章节引用、阶段状态、F07/F09 覆盖关系、配置时间起点、授权周期终点。
- 6 个 P1 应在同一轮清理；修订后重点回归 AC-F07-05/06、AC-F09-07/08/15、§0.5、§8.5、§9.7.G、§10.0、§12.7 与 §13。

---

## R4′ 回归结果

复核对象：修订后的 `output/PRD详细版.md`（当前 3009 行）。本节保留上方首轮问题作为历史摘要；以下状态和行号取代首轮结论。

### 原 11 项逐项复核

| # | 原问题 | 状态 | 当前证据与判断 |
| :--- | :--- | :--- | :--- |
| 1 | C3.1 章节引用漂移 | **CLOSED** | `:53`、`:75` 已改引 §0.5；`:1616` 改引 §9.7.G；`:2126`、`:2150`、`:2737` 统一引用 §0.5/§8.5；`:2949` 的 NFR-010 改引 §9.7.G。 |
| 2 | C4.1 阶段 2—4 状态冲突 | **OPEN · P1** | `:5`、`:101`、`:2805` 已区分 F01—F08 原始 V0 与 F09；但 `:2817-2823` 仍无作用域限定地写“阶段 2/3/4 未执行”，与 `:2834-2838` 的 F09 已完成阶段材料并列，局部仍可被读成全项目状态。 |
| 3 | C4.2 F07 与 F09 的配置责任/无角色行为冲突 | **OPEN · P0** | 新口径已见 `:30`、`:192`、`:2729`、`:2827`、`:3002`；但核心 FR-7 仍在 `:1241`、`:1246`、`:1253`、`:1264-1265` 指定运维配置且无角色通用拒绝，依赖表 `:2175`、`:2188` 及 AC-F07-05/06 `:2683`、`:2689` 仍重复旧口径。仅在 AC-F09-07 `:2729` 声明“替换”不足以消除实施与验收冲突。 |
| 4 | C5.1 配置时间起点不唯一 | **CLOSED** | `:1518-1519`、`:1529` 明确由确认动作写入不可回填的服务端 `configuration_started_at`，外部实际取得时间仅作证据；`:1618`、`:2134` 使用同一指标公式。 |
| 5 | C5.2 停用周期终点不可达 | **OPEN · P1** | 指标表 `:864`、`:2135` 已明确停用止于事务成功且不纳入完整授权指标；但交互规则 `:1619` 仍要求“新增、变更、停用和恢复”都关联“下一次正确角色工作台访问”，停用场景没有该可达终点。 |
| 6 | C2.1 恢复通知状态命名漂移 | **OPEN · P1** | 数据模型 `:832` 使用待投递/已投递/失败待补偿/补偿已闭环；页面 `:1428` 同此四态，但流程 `:1889` 使用“通知待处理”，风险文案 `:2399` 使用“通知失败待补偿”，AC-F09-06 `:2751` 使用“安全通知处置失败待处理/已闭环”，尚无规范词与展示词映射。 |
| 7 | C3.2 F09 用户故事引用不存在的 FR-9 | **CLOSED** | US-F09-01—03 已分别在 `:272`、`:276`、`:280` 引用实际存在的 FR-9.1—9.7 子项；§13 `:2988-2990` 同步一致。 |
| 8 | C3.3 §10.0 未收录 F09 AC | **CLOSED** | `:2460-2474` 已完整收录 AC-F09-01—15，且保留既有 AC 编号，不重排稳定 ID。 |
| 9 | C3.4 NFR-008/009 错引 AC-F09-15 | **CLOSED** | `:2793-2794` 已移除 AC-F09-15；时间验收仍只在 AC-F09-15 `:2737`。 |
| 10 | C3.5 §13 漏 F09 且旧口径仍标 PASS | **OPEN · P1** | F09 三条故事与跨章节项已补入 `:2988-2990`、`:3002-3006`；但 `:3002` 将新身份口径标为 PASS，而核心 FR/AC 在 `:1241-1265`、`:2683-2689` 仍保留相反旧口径。覆盖缺口已关闭，PASS 真实性依赖第 3 项修复。 |
| 11 | C4.3 技术栈既确认又不可虚构 | **CLOSED** | `:2908` 已将未知项限定为 LiteLLM 网关地址、认证协议和模型参数，并明确 Go、React + TypeScript、PostgreSQL 已确认。 |

原 11 项当前为 **6 closed、5 open**；未关闭项含 **1 个 P0、4 个 P1**。

### C1—C6 再扫描新增冲突

#### NEW-C3.1 · P1：完整授权周期引用了错误章节

- `:1618` 写“完整授权周期按操作类型使用 §8.5 的起点与终点”。
- §8.5 `:2138` 起只定义首次启用七里程碑；按操作类型的首次授权、变更、恢复与停用公式实际位于 §8.4 `:2135`。
- **最小修复**：把 `:1618` 的“§8.5”改为“§8.4 指标表”；首次启用 10 分钟继续引用 §8.5。

#### NEW-C4.1 · P1：D/E 证据使用声明的作用域过宽

- `:122` 全局声明“本稿未使用 D、E 级材料形成业务结论”。
- F09 随后在 `:150` 使用 D 级的 12 次/年与三年持续性形成风险暴露上限，在 `:161`、`:163` 使用 B/E 支撑风险方向，且 `:2844-2849` 明列 E 级外部研究。
- 这些内容均已写出限制，不构成把假设冒充事实；冲突在于 `:122` 的绝对表述。
- **最小修复**：将 `:122` 限定为 F01—F08 原始 V0，或改为“D/E 不形成已确认事实、收益承诺或合规结论；F09 仅按显式限制用于待验证风险模型与方向性佐证”。

其余扫描：C1 数字/数量无新增冲突；C2 除原第 6 项外无新增冲突；C5 除原第 5 项残留外无新增冲突；C6 单位/量纲无新增冲突。

### 仍未关闭的最小修复清单

1. **P0**：统一 FR-7、§9.B/§9.C、AC-F07-05/06 与 F09。保留 AC-F07-05“已有唯一有效映射正常登录”的行为，但把配置执行者改为唯一 Admin；把 AC-F07-06 拆清：认证失败/会话失效继续拒绝，无唯一角色改由 AC-F09-07/08 进入待处理，不再联系运维配置。
2. **P1**：在 `:2817-2823` 明确前三行仅描述 F01—F08 原始 V0。
3. **P1**：从 `:1619` 的“下一次正确角色工作台访问”要求中排除停用，改为停用止于事务成功。
4. **P1**：确定通知处置规范枚举及唯一中文展示映射，统一 `:832`、`:1428`、`:1889`、`:2399`、`:2751`。
5. **P1**：第 1 项修好后保留 `:3002` 的 PASS；修复前应标 FAIL/待修复。
6. **P1**：修正 `:1618` 的 §8.5→§8.4 引用，并收敛 `:122` 的 D/E 声明。

## R4′ 结论

- ❌ 仍不建议进入阶段 5.5 终审或发布：当前尚有 **1 个 P0、6 个 P1**（含本轮新发现 2 个 P1）。
- 阻塞发布的唯一 P0 是 F07→F09 增量覆盖未落入核心 FR、外部责任表和旧 AC；其余问题均可用局部文案/引用修订解决，无需改变已确认产品边界。

---

## R4″ 最终回归结论

复核对象：最新 `output/PRD详细版.md`（3009 行）、`output/requirements.md` revision 11、`output/requirements-analysis.md`（65 行）。本节为最终有效结论，上方 R4/R4′ 内容仅保留问题演进历史。

### 上轮遗留 1 P0、6 P1 复核

| 上轮遗留 | 最终状态 | 当前证据 |
| :--- | :--- | :--- |
| P0：F07→F09 配置责任与无角色行为未统一 | **OPEN · P0** | 核心 FR/AC 已修正：PRD `:1241-1265`、`:2175`、`:2188`、`:2683-2689` 与 requirements `:761`、`:814-826` 均统一为“事业部负责人审批、唯一 Admin 配置；无角色进入待处理”。但 PRD 角色矩阵 `:184` 仍写运维“按事业部负责人审批结果配置双角色映射”；requirements-analysis `:30` 仍写“运维/系统管理员配置；无双角色映射时默认拒绝”，并与同文件 `:37-38` 的 F09 接管/Admin 配置直接矛盾。 |
| P1：阶段 2—4 作用域未限定 | **CLOSED** | PRD `:2821-2823` 已逐行限定为“F01—F08 原始 V0”；`:5`、`:101`、`:2805` 与 requirements-analysis `:7` 使用同一增量口径。 |
| P1：停用要求不可达的工作台终点 | **CLOSED** | PRD `:864`、`:1619`、`:2135`、`:2926` 均明确停用止于事务成功且不纳入完整授权周期；requirements-analysis `:43` 同步。 |
| P1：恢复通知状态命名漂移 | **CLOSED** | PRD `:832`、`:1428`、`:1436`、`:1889-1894`、`:2399`、`:2751` 已统一为 `pending_delivery / delivered / failed_pending_compensation / compensated_closed` 及唯一中文展示词；requirements-analysis `:53` 使用同一闭环终态。 |
| P1：§13 身份口径错误标 PASS | **OPEN · P1** | PRD `:3002` 的目标口径本身已正确，但仍标 PASS；PRD `:184` 与 requirements-analysis `:30` 尚保留相反责任/无角色口径，因此该 PASS 仍不真实。随上项 P0 修复后即可关闭。 |
| P1：完整授权周期误引 §8.5 | **CLOSED** | PRD `:1618` 已改引 §8.4；§8.5 只承载首次启用七里程碑。 |
| P1：D/E 使用声明范围过宽 | **CLOSED** | PRD `:122` 已明确 F01—F08 与 F09 的不同证据用法；`:150` 的 72 万/216 万仍只作为待验证风险暴露/可支撑价值上限。 |

上轮 7 项最终为 **5 closed、2 open**。

### C1—C6 全量复扫新增开放项

#### NEW-C3/C4.1 · P0：10 分钟指标引用的完整上线门禁不唯一

- PRD `:57` 声明 §0.5 是“F09 第一版完成的唯一完整门禁清单”，`:75` 明确为十项检查；`:53`、`:2126`、`:2150`、`:2737`、`:3006` 均要求 §0.5 全部门禁满足后才能判断 10 分钟。
- PRD `:1616` 却改用“§9.7.G 全部生产上线门禁”，`:862` 写“§0 全部门禁”，`:2314` 又写“PM §0 的完整门禁清单”。§9.7.G `:2387-2401` 是按硬门禁、操作级门禁、治理门禁重新归组的实现清单，不是 §0.5 的十项逐项清单。
- **影响**：实现和验收可选择不同门禁集合，导致同一次首次启用被一处判定可计量、另一处判定不可计量。
- **最小修复**：所有“首次闭环/10 分钟有效性”的门禁引用统一为 §0.5；§9.7.G 只保留实现分层说明，并明确不得替代 §0.5。

#### NEW-C2.1 · P1：第二个授权里程碑在 PRD 与机器契约名称不同

- PRD `:814-820`、`:2142-2148` 将第 2 个里程碑定义为“Admin 创建成功”。
- requirements `:1143` 写成“初始化提交”；提交动作可能早于事务成功，不能天然等同于 Admin 已创建。
- **最小修复**：requirements AC-F09-15 将“初始化提交”改为“Admin 创建成功”，并继续以服务端成功事件计时。

#### NEW-C4.2 · P1：已验证的外部能力仍在依赖章节写成待验证

- PRD `:28` 写 LiteLLM 生产连通性与合规重试已验证，requirements `:26` 状态为 `ready`；PRD `:2187` 仍写“运行可用性待验证”。
- PRD `:31` 写钉钉机器人真实投递已验证，requirements `:28` 状态为 `ready`；PRD `:2189` 仍写“接入可用性待实施验证”。
- **最小修复**：把 `:2187`、`:2189` 的当前状态同步为已验证；保留凭据轮换、失败告警等上线运维责任，不扩大功能范围。

### 六类最终结果

| 类别 | 结果 | 说明 |
| :--- | :--- | :--- |
| C1 数字/数量 | PASS | 10—20 个 SPU、至少 3 个问题、单 Admin、两个可靠属性、七里程碑、10 分钟、30 天、12 次/年、72 万/年与三年 216 万的数量及限制未发现新增冲突 |
| C2 术语/命名 | **P1** | 第二里程碑“Admin 创建成功/初始化提交”未统一 |
| C3 引用/链接 | **P0** | 首次启用指标在 §0.5、§0、§9.7.G 之间引用分叉 |
| C4 决策一致性 | **P0** | 角色配置责任残留旧口径；外部能力 ready/待验证状态冲突 |
| C5 时间口径 | PASS | `configuration_started_at`、完整授权周期、停用终点、七里程碑首尾与 `Asia/Shanghai` 假设已一致 |
| C6 量纲/单位 | PASS | 金额、比例、SPU 数量与分钟/天/年单位未发现冲突；72 万和 216 万未被写成已实现收益 |

### 校验旁证与边界

- `check_evidence.py`、`check_format.py`、`check_requirements_contract.py` 均通过；`check_executability.py` 为 560/600（93%）。这些机械校验不覆盖上述语义冲突。
- `check_consistency.py` 在本机 Python 3.9 因脚本使用 `str | None` 语法未能启动；这是校验器运行时兼容问题，不作为文档通过证据，也不计入产品 P0/P1。
- requirements-analysis `:64-65` 自报 `P0 unresolved: 0 / P1 unresolved: 0`，但与其 `:30`、`:38` 的直接矛盾不相容，不能据此放行。

## R4″ 最终判断

- ❌ **不通过**：当前真实剩余 **2 个 P0、3 个 P1**。
- 先统一角色配置责任/无角色行为和 10 分钟门禁唯一引用，再同步里程碑名称、依赖状态及 §13/requirements-analysis 自检结果；完成后仍需一次只针对这些当前行号的最小回归。

---

## R4‴ 最后定点回归

本轮只复核 R4″ 的 2 个 P0 与 3 个 P1，不扩展扫描范围。以下结论取代 R4″ 的开放状态。

| R4″ 开放项 | 状态 | 最终证据 |
| :--- | :--- | :--- |
| P0：PRD 角色矩阵与 requirements-analysis 仍由运维配置业务角色 | **CLOSED** | PRD `:184` 已改为运维只维护身份接入、恢复材料和外部凭据，明确“业务角色由唯一 Admin 按事业部负责人审批结果配置”且运维“不得审批或配置业务角色”；`:192` 同口径。requirements-analysis `:30` 已同步为唯一 Admin 配置、无角色进入 F09 待处理，并与 `:37-38` 一致。 |
| P0：10 分钟指标的完整门禁引用分叉 | **CLOSED** | PRD `:862`、`:1616`、`:2126`、`:2150`、`:2314`、`:2737`、`:3006` 均统一以 §0.5 十项门禁为唯一有效条件；`:1616`、`:2314` 明确 §9.7.G 只解释实现分层、不能替代 §0.5。 |
| P1：第二里程碑“Admin 创建成功/初始化提交”不一致 | **CLOSED** | PRD `:815`、`:2143` 与 requirements AC-F09-15 `:1143` 均使用“Admin 创建成功”，不再以提交动作替代成功事件。 |
| P1：LiteLLM/机器人 ready 状态冲突 | **CLOSED** | PRD `:2187` 已改为“已完成生产链路验证”，`:2189` 已改为“已完成真实投递与幂等验证”；分别与 PRD `:28`、`:31` 及 requirements `:26`、`:28` 的 ready 证据一致。 |
| P1：§13 身份口径错误标 PASS | **CLOSED** | PRD `:3002` 的“事业部负责人审批、唯一 Admin 配置、运维维护接入与恢复；无角色进入待处理”已与角色矩阵 `:184`、协作结论 `:192`、requirements `:761` 及 requirements-analysis `:30` 一致，PASS 具备事实基础。 |

### 最终结果

- **P0 = 0**
- **P1 = 0**
- ✅ R4″ 指定的全部开放项已关闭，最后定点回归通过。

---

## 最终 R4 全量跨章节复核（最新 3019 行版本）

复核对象：最新 `output/PRD详细版.md`（3019 行）、`output/requirements.md` revision 11（1216 行）和 `output/requirements-analysis.md`（70 行）。本轮不是定点回归，已重新扫描 C1—C6；本节结论取代上方历史结论。

### 总结

- **P0 = 2**
- **P1 = 1**
- **结论：不通过。** 上轮 R4″ 的 2 个 P0 + 3 个 P1 仍保持关闭；本轮专项修订新引入/暴露 2 个直接矛盾，并有 1 个机器契约追溯缺口。

## 🔴 P0 必须修复

### FINAL-C4.1｜密码“永不回显”与“仅当次看到新密码/长期凭据”同时有效

- PRD `output/PRD详细版.md:1324` 明确：“**密码**、setup/recovery 原始口令和完整会话令牌**永不回显**；仅 TOTP 绑定二维码/手工密钥与新恢复码允许 `no-store` 当次展示。”
- 同一 PRD `output/PRD详细版.md:1443` 又写 Admin 持有人“设置并**仅当次看到新密码**、TOTP 密钥和恢复码”。
- 机器契约 `output/requirements.md:947` 写“设置并仅当次看到**长期凭据**”，但 NFR-007 `:1195` 再次规定密码永不回显、只允许 TOTP 密钥/二维码和新恢复码单次展示。
- 正确性分析也自相矛盾：`output/requirements-analysis.md:41` 写“看到新长期凭据”，`:45` 写“密码……不回显，TOTP 绑定密钥和新恢复码单次展示”。
- **影响**：设计和实现无法判断恢复时能否通过响应展示密码；若按“看到新密码”实现，会直接违反凭据保护 NFR。
- **最小修复**：统一为“Admin 持有人本人在掩码输入框中设置新密码，系统永不返回或重显密码；仅 TOTP 绑定密钥/二维码与新恢复码可在受保护 `no-store` 绑定当次展示”。同步修正 PRD `:1443`、requirements `:947`、requirements-analysis `:41`；保留 `:1324`、NFR-007 和 AC-F09-01/05/06 的严格口径。

### FINAL-C2/C4.1｜恢复通知补偿的操作人和入口权限互相排斥

- PRD `output/PRD详细版.md:1444` 指定“**恢复执行人提交**……补偿证据引用，独立安全责任岗位确认后”闭环。
- 但产品入口 `output/PRD详细版.md:1431`、`:1575` 均把“安全设置/审计详情”的恢复通知处置限定为**有效 Admin 会话**，非 Admin 禁止；角色边界 `:251`、长期凭据边界 `:1443` 又明确恢复执行人不取得或共享 Admin 长期凭据。
- requirements `output/requirements.md:947,1044` 只写“页面退出后仍可返回/提交补偿证据引用”，没有指定哪个授权主体执行，未消除 PRD 的角色冲突；requirements-analysis `:41` 也只说执行人与通知岗位看到元数据。
- **影响**：通知失败后，规范指定的恢复执行人无法进入产品入口，唯一能进入的 Admin 又未被指定为提交人，`compensated_closed` 可能没有可执行的责任闭环；这直接影响 F09 P0 的非静默恢复完成定义。
- **最小修复**：二选一并全文统一，不新增通知中心：① 推荐由恢复执行人在受控恢复会话存续期提交证据引用，恢复页退出后由 Admin 只读追踪/由独立安全岗位通过组织外渠道确认并由系统投影；或 ② 由恢复执行人在组织外工单提供证据，恢复后的 Admin 在安全设置录入引用，独立安全岗位确认。必须明确提交、确认、查看三种动作各自的认证主体，并同步 REQ-F09-03、AC-F09-06 与 requirements-analysis。

## 🟠 P1 建议修复

### FINAL-C3.1｜正确性分析宣称 setup 使用稳定幂等键，但 AC-F09-02 未形成该机器契约

- PRD `output/PRD详细版.md:782,853` 要求 setup、恢复和角色写入携带幂等请求标识，结果未知时查询 `request_id/result_ref`。
- requirements-analysis `output/requirements-analysis.md:47,57` 明确把 **setup** 纳入“稳定幂等键”，并引用 AC-F09-02。
- 但 requirements 的 AC-F09-02 `output/requirements.md:999-1000` 只描述重复/并发/网络中断时“不产生第二 Admin、已有成功结果返回完成状态”，没有要求稳定幂等键，也没有要求返回可判定的同一已提交/未提交结果；AC-F09-05/06/12 已明确写入稳定幂等键，唯独 setup 缺失。
- **影响**：单 Admin 唯一约束能防止第二管理员，但实现仍可能无法把响应丢失后的重试关联到同一初始化结果，requirements-analysis 对 AC 的 PASS 缺少机器契约依据。
- **最小修复**：在 AC-F09-02 的触发中加入稳定幂等键，在结果中加入“重放返回同一已提交/未提交结果，不重复消费材料、创建 Admin 或写入审计”；然后才保留 requirements-analysis `:47,57` 与 `P0/P1 unresolved: 0`。

## 五组专项核验

| 专项 | 结果 | 当前统一证据 |
| :--- | :--- | :--- |
| 秘密单次展示 | **P0** | AC-F09-01/05/06 与 NFR-007 已统一限定 TOTP 密钥/二维码和新恢复码 `no-store` 当次展示，但 PRD `:1443`、requirements `:947`、analysis `:41` 把密码/长期凭据重新纳入“看到”范围。 |
| 配置冲突解除 | PASS | PRD `:1527,1543,2765`、requirements `:963,1088`、analysis `:44` 均区分“产品内双映射由 Admin 基于新审批原子选一”与“身份源/作用域/组织属性由权威源责任岗位纠正”，不提供身份合并。 |
| 恢复通知处置 | **P0** | 通知四态和返回入口已一致，但补偿证据提交人的身份与入口权限矛盾。 |
| 管理写安全、审计权限、幂等 | **P1** | CSRF + 严格 Origin + Content-Type 在 PRD `:1588`、AC-F09-13 `requirements.md:1120-1121`、NFR-006 与 analysis `:46` 一致；运行身份无审计 UPDATE/DELETE/DDL 在 PRD `:1588,2795`、AC-F09-14/NFR-008、analysis `:46` 一致；恢复和角色写入幂等一致，仅 setup AC 缺稳定键。 |
| 商业终审负向停止线 | PASS | PRD `:165-169` 明确 30 天真实记录若显示申请极少且无可归因经营影响，则不启动 OA、周期复核、导出等高级治理投资；不虚构阈值、不为维持价值假设扩张。该决策不要求产品自动化，未被 requirements 或 analysis 写成相反行为。 |

## C1—C6 全量扫描结果

| 类别 | 结果 | 说明 |
| :--- | :--- | :--- |
| C1 数字/数量 | PASS | 单 Admin、2 个业务角色、至少 2 个可靠属性、5 个持久状态、6 项 MVP、7 个里程碑、10 分钟、30 天、12 次/年、72 万元/年与三年 216 万元口径及限制一致。 |
| C2 术语/命名 | **P0** | 通知四个机器状态已统一；但“恢复执行人”与“有效 Admin”对同一补偿提交动作的角色命名/权限不一致。 |
| C3 引用/追溯 | **P1** | §0.5、§8.4/8.5、§9.7.G 与 F09 稳定 ID 引用未发现新漂移；setup 稳定幂等键由 analysis 引用 AC-F09-02，但 AC 实文未承载。 |
| C4 决策一致性 | **P0** | 配置冲突、管理写防护、审计数据库权限和范围停止线一致；密码展示边界直接冲突。 |
| C5 时间口径 | PASS | `configuration_started_at`、首次/变更/恢复周期、停用终点、绝对事件时间及 `Asia/Shanghai` 实现假设未发现新冲突。 |
| C6 量纲/单位 | PASS | SPU 数量、百分比、人民币金额及分钟/天/年单位一致；72 万元与 216 万元仍未被写成已实现收益。 |

## 校验旁证

- `check_format.py`：通过。
- `check_evidence.py`：通过。
- `check_requirements_contract.py requirements.md requirements-analysis.md`：通过，稳定对象 122 个；该校验只检查结构和 `P0/P1 unresolved: 0` 字符串，不识别上述语义矛盾。
- `check_executability.py`：560/600（93%），适合编码助手开工；不覆盖跨章节角色、秘密展示和幂等语义。
- `check_consistency.py`：本机 Python 3.9 因 `str | None` 语法无法启动，不作为 PASS 证据。

## 最终判断

- ❌ **不通过：P0 = 2，P1 = 1。**
- 先收敛密码永不回显和通知补偿责任/权限两项 P0，再把 setup 稳定幂等键写入 AC-F09-02；同步更新 requirements-analysis `:62,69-70` 的清零结论后，执行一次最小回归即可。

---

## 最终 R4 定点回归（2 P0 + 1 P1）

本轮仅复核上一节的 2 个 P0 与 1 个 P1，不扩展扫描范围。复核对象仍为 `output/PRD详细版.md`（3019 行）、`output/requirements.md` revision 11（1216 行）和 `output/requirements-analysis.md`（70 行）；以下状态取代上一节开放结论。

| 上轮开放项 | 状态 | 最新证据与判断 |
| :--- | :--- | :--- |
| P0：密码永不回显与“看到新密码/长期凭据”冲突 | **CLOSED** | PRD `:1324,1443` 已统一为 Admin 持有人本人输入新密码、系统按密码字段处理且不回显，仅 TOTP 绑定密钥和恢复码当次展示；requirements `:947,1195` 与 requirements-analysis `:41,45` 使用同一边界。未再检出“看到新密码”或泛化为“看到长期凭据”的口径。 |
| P0：恢复通知补偿操作人与入口权限互斥 | **CLOSED** | PRD `:1444` 已明确恢复执行人在产品外受控工单生成证据引用，恢复后的 Admin 只在受限入口查看状态或代录引用，独立安全责任岗位在产品外核验并回写确认；`:1431,1575` 的有效 Admin 入口限制与该分工一致。requirements `:947` 已明确 Admin 查看/代录及外部工单来源，requirements-analysis `:41` 同步；恢复执行人不再被要求使用 Admin 入口或取得长期凭据。 |
| P1：setup 稳定幂等键未下沉 AC-F09-02 | **CLOSED** | PRD AC-F09-02 `:2751-2753` 和 requirements AC-F09-02 `:992-1000` 均明确同一初始化使用稳定幂等键，重放返回可判定的同一已提交/未提交结果，且不重复创建 Admin、消费材料或追加审计；requirements-analysis `:47,57` 的引用现有机器契约支撑。 |

### 定点回归结论

- **P0 = 0**
- **P1 = 0**
- ✅ 上轮 2 个 P0 与 1 个 P1 全部关闭，最终定点回归通过。
- `requirements-analysis.md:69-70` 的 `P0 unresolved: 0 / P1 unresolved: 0` 与当前三项复核结果一致。
- 旁证：`check_requirements_contract.py` 通过（稳定对象 122 个）；`check_format.py` 通过。机械校验只作旁证，关闭判断以上述逐项语义证据为准。
