# TDD 验收结果 · 趣然 AI 商品经营与利润决策助手

- contract_sha256: sha256:b35d24064fb0031a952ac73bff7b5b7ecd97795963f44e40bf99af0021fc44c8
- run_started_at: 2026-08-04T23:46:33+0800

| TDD ID | 状态 | 动作 | 证据 |
| :--- | :--- | :--- | :--- |
| SMOKE-01 | pass | 在干净克隆的钉钉候选提交上，使用全新 npm/Go 模块与编译缓存、空 PostgreSQL 数据库执行锁文件安装、OpenAPI 无差异、React/Go 全门禁；再构建并启动隔离生产 Compose，用 HTTP 与隔离浏览器实测健康、登录和未配置钉钉默认拒绝 | 干净克隆 `/tmp/profit-dingtalk-gate-BDPSo3` @ `bd95494`；全新 npm cache 上 `npm ci`=306 packages/0 vulnerabilities，OpenAPI diff、lint、typecheck、Vitest、build 均 exit 0；公网 Go proxy 首次超时后，以锁定的本机 module download cache 作为只读 `file://` 代理写入全新 `GOMODCACHE/GOCACHE`，`go mod download`、空库 goose 00001–00005、gofmt、`go vet ./...`、`go test -race -count=1 ./...`、`go build ./cmd/...` 均 exit 0；`profit-decision-dingtalk-gate` Compose 构建/启动 exit 0，db healthy、api/worker/web Up、migrate Exited 0，`/health/ready`=200 ready、`/health/live`=200 ok、`/login`=200 React、`/api/session`=预期 401，web/api/worker 均无 Node Runtime；隔离浏览器 `dingtalk-final-gate` 显示完整钉钉登录页，点击真实 `/auth/dingtalk/start` 后在无配置时进入只含恢复信息的 `/auth/recovery?reason=network`，无经营数据；控制台仅预期会话探测 401，服务日志无 panic、配置密钥或 Access Token 明文 |
| SMOKE-02 | pending | 已完成用户真实 XLSX 的生产 API→Worker→PostgreSQL 导入和双角色核心闭环；以用户更换后的真实 Client 配置重新验证到钉钉官方授权页，等待测试钉钉身份与含 SPU 515 的真实 2026-06 验收工作簿 | 用户 `商品链接.xlsx` sha256=`af37a29543bf5a37117985e70619bee2534190a22655359d6e005ea80058cf5f`，生产 API 真实导入后为 1 batch/10 snapshots/10 decisions/7 tasks/7 links；已持久化 10 个 SPU 中 `515` 命中 0；更换凭据后强制重建隔离空库候选 `profit-decision-real-auth`，真实入口返回 302 到 `login.dingtalk.com/oauth2/auth`，参数键和一次性 state cookie 齐全；仍无可完成回调的测试身份，且工作簿无 515，故不改写 XLSX、不硬编码 515、不伪造登录并保持 pending |
| FLOW-01 | pending | 已完成钉钉 OAuth HTTP 契约、默认拒绝、真实 PostgreSQL 权限/会话矩阵与更换后真实 Client 配置的官方授权页跳转；等待钉钉测试身份完成真实回调 | `go test -race ./internal/identity -count=1 -v` 和 `go test -race ./internal/httpapi -run TestSafeReturn -count=1 -v` exit 0，覆盖 OAuth 协议、运营/主管/无角色/撤权/超时与恶意返回地址；新 Client ID/Secret 调用官方 Access Token 接口 HTTP 200/有效期 7200 秒，隔离候选重建后真实 OAuth 返回 302 至钉钉官方页，全程不输出 Secret/token；旧应用名称证据已失效，当前又无可用用户浏览器检查新应用名或完成授权回调，且无真实 unionId 及站内已审批角色映射，故保持 pending |
| FLOW-02 | pass | 对真实 XLSX 的合法导入、幂等/并发、非法期间、身份缺失、重复 SPU 与经营字段降级执行 API/数据库测试，并在 RC2 浏览器复核批次状态和持久化详情 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 TEST_XLSX_PATH=…/商品链接.xlsx go test -race -count=1 -v ./internal/batch` exit 0；8 路并发仅 1 个非幂等结果、同一 batch_id、批次/清单 1/1，非法期间持久化 0，重复/身份错误拒绝且经营异常保留定位；RC2 生产 API 导入生成 `BATCH-20260630-4EC92F6A`，浏览器 `/batches` 与 `/batches/4ec92f6a-a1e2-4317-abda-551cd1187c56` fresh 复核 1 批次、10 有效/0 拒绝/10 降级、文件 `商品链接.xlsx`、10 条真实 SPU 与逐字段问题证据 |
| FLOW-03 | pass | 执行指标边界、异常与汇总行测试，并在真实批次证据抽屉和建议详情复核来源、原值、周期及缺失语义 | 同一 `go test -race -count=1 -v ./internal/batch` exit 0；品退率=1/100=0.01、库存天数=(100+20)/(70/14)=24、源利润率=0.1，零分母/负库存/非法利润为 nil+独立质量态，汇总行排除且差异告警；RC2 浏览器批次详情打开真实证据抽屉，显示源行、原始列值、结构化规则证据和影响；建议详情 `/suggestions/2b15249e-fb77-4fab-b214-59e26364b438` fresh 显示销售 `¥56,461.5`、利润率 `7.60%`、近 7 日品退率“未校验”、库存天数“数据不足”，未补 0 |
| FLOW-04 | pass | 先红后绿执行全分类/阈值/动作/缺字段矩阵及数据库原子组合，并在真实清单与详情复核规则结果 | 旧提交 `6e45b81` 的 3 个 `watch` 分支 `go test` exit 1；当前 `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 TEST_XLSX_PATH=…/商品链接.xlsx go test -race -count=1 -v ./internal/batch` exit 0，13 个边界+5 个证据不足分支全过，真实批次补货/禁补矛盾=0、遗留 watch=0；RC2 浏览器行动中心固定顺序显示 2 条清仓后 1 条止损，详情分别展示冻结触发规则与 `清仓/止损 + 禁止补货`，真实 10 决策/7 任务均来自 `RULE-V1.0` |
| FLOW-05 | pass | 先执行 LiteLLM HTTP 故障/越界矩阵和未配置模型的浏览器降级闭环，再以真实 XLSX、真实网关和生产重试 API 验证失败后恢复 | `go vet ./internal/explanation ./cmd/worker`、`go test -race ./internal/explanation -count=1`、`go build ./cmd/worker` 均 exit 0；隔离 Compose `profit-decision-real-auth` 通过生产 API 导入原文件为 `BATCH-20260630-069C37CE`，SHA 与用户文件一致，形成 10 snapshots/10 decisions/7 links。首轮真实模型调用 6 条 `not_adopted`、4 条 `litellm_transport_failed` 且 0 条越界原文入库；修正为精确动作值/字符串形状提示并将实测不足的 20 秒超时调整为 45 秒后，7 个 `POST /api/suggestions/:linkID/ai-retry` 均 HTTP 200，1 条传输失败再次通过同一 API 重试，最终 7/7 行动建议最新解释为 `generated`、形状错误 0、动作冲突 0；决策摘要始终 `9e0d6e2a3a5816cbcf34843f82f0bb83`，任务摘要始终 `1b78ba7edc86adc25d5aba159a677d6c`，业务状态未被 AI 修改，worker 日志对 LiteLLM key/钉钉 Secret 值扫描命中 0 |
| FLOW-06 | pass | 执行页签/组合筛选/角色范围/分页状态机，并以最新 RC2 镜像完成运营与主管双角色浏览器 E2E | `TestActionTabsAndFiltersFollowRoleProgress` 与独立 PostgreSQL `go test -race ./internal/action -count=1` exit 0；RC2 运营工作台 fresh 显示真实批次、本人 4 个 SPU 的两类数据限制各影响 4 个；行动中心四页签为待我处理/全部/处理中/已完成，`search=745838943806&action=stop_loss` 唯一返回真实 SPU 且 URL 保留条件；主管工作台显示 7 条待审核且仅有逐条入口、无批量写；执行后待我处理、状态与生命周期时间同步更新 |
| FLOW-07 | pending | 已完成真实 PostgreSQL 的三周稳定任务/快照字段集成验证；浏览器已可用，但等待同一真实 SPU 连续三周依次产生清仓、清仓、观察的三份真实工作簿，才能执行生产 API 与页面 E2E | `TestActionLifecycleContinuesStableTaskAndStagesChangedAction` exit 0：三周仅 1 个稳定任务、3 条周关联、2 个动作版本，第二周不重置审核/执行/结果，第三周 `previous_link_id` 精确指向第二周且冻结字段逐项一致；用户真实 `商品链接.xlsx` 仅提供 2026-06 单期，无另外两周的真实快照；不用合成 fixture 冒充浏览器生产验收，故保持 pending |
| FLOW-08 | pass | 执行审核状态机、幂等/并发与错误响应，并由主管浏览器逐条通过真实双轨建议 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 go test -race -count=1 -run 'TestSupervisorReview|TestSupervisorRejection|TestErrorResponses' -v ./internal/action ./internal/httpapi` exit 0，覆盖通过、驳回必填、无权、重复、旧版本 409 与最新操作者/时间；RC2 主管浏览器 `/suggestions/8749b4e8-46aa-4af1-92ac-e4aa983c4765` fresh 提交备注并确认整体通过，状态 `pending→approved`，经营/库存同时 `pending_execution`，时间线仅追加 1 条 `suggestion_review`，运营随后才获得执行入口 |
| FLOW-09 | pass | 先用真实浏览器复现待审核改判错误激活双轨，再修正改判/审核状态隔离；执行空理由、沿用禁补、三类库存选择、待审核/已通过状态保持、已执行先终止、版本/幂等和历史重放测试，并以另一条真实 SPU 完成“清仓+禁补→加投+不补货→整体通过”浏览器闭环 | 修复前 RC2 SPU `3777853568762118406` 改判后错误显示 `approved/pending_execution`，未作为通过证据；修复后 `TEST_DATABASE_URL=postgres://…/candidate_tests_override_2253 go test -race ./internal/action ./internal/httpapi -count=1` exit 0，pending 改判保持 `pending/pending_review`，approved/terminated 改判保持 `approved/pending_execution`，restock/no_restock/no_coordination 3/3、直接已执行拒绝和先终止后改判均 pass；RC2 浏览器 fresh 在 SPU `3798495589524701423` 保留原规则“清仓+禁止补货”，空理由按钮禁用，沿用禁补提交失败且输入/状态不变，改选“加投+不补货”后仅追加 `supervisor_override` 且仍为待审核/双轨 pending_review，再单独整体通过后双轨才变 pending_execution，时间线依次记录 `supervisor_override` 与 `suggestion_review`、操作者 `rc2-supervisor` 及两条真实理由 |
| FLOW-10 | pending | 已以精确钉钉 HTTP 协议完成公司 User ID、成功受理、三类收件人失败、429 不自动重试、人工补发及双轨/结果持久化集成；等待真实机器人和收件账号完成外部实际收件 | 新空库 `dingtalk_tests_2357` 迁移 00001–00005 成功；`go test -race -count=1 ./cmd/... ./internal/...` exit 0；`internal/oa` 断言 token 请求 `appKey/appSecret`、消息请求 `robotCode/userIds/msgKey/msgParam`、请求标识及无副作用重试。2026-08-05 01:18 fresh 调用当前 Client ID/Secret 获取 Access Token HTTP 200/有效期 7200 秒；因 `.env` 无 `DINGTALK_ROBOT_CODE`，仅用 Client ID 和明确不存在的验收 User ID 做无真实收件人的负向检查，官方消息接口返回 HTTP 400 `invalidParameter.robotCode.notExsit`。当前先缺实际机器人编码；提供后还须复核 `qyapi_robot_sendmsg` 权限并用公司 User ID/实际账号验收收件，故保持 pending |
| FLOW-11 | pending | 已在真实 PostgreSQL 用 `role_mapping.dingtalk_user_id` 完成跨两个 Asia/Shanghai 自然日的并发唯一提醒、失败补发、确认/改判/终止停催；等待真实责任运营 User ID 的钉钉实际收件 | 00005 新增非空约束和活跃 User ID 唯一索引；`go test -race -count=1 ./cmd/... ./internal/...` exit 0，催办协议调用只使用 `dingtalk_user_id`，不再误用登录 Union ID；2026-08-05 01:18 当前应用凭据取 token 成功，但在无实际 `DINGTALK_ROBOT_CODE` 时官方消息接口返回 HTTP 400 `invalidParameter.robotCode.notExsit`，且尚无责任运营公司 User ID，无法形成真实收件证据，保持 pending |
| FLOW-12 | pass | 执行历史组合检索、快照重放、角色裁剪与不可变更新拒绝，并完成浏览器组合查询和只读详情 E2E | `TEST_DATABASE_URL=postgres://…@127.0.0.1:55434/profit_decision?sslmode=disable go test -race ./internal/action -count=1` exit 0，覆盖改判后冻结 RULE-V1.0、通过/驳回/部分执行/冲突/AI 失败投影、原位更新拒绝及非责任运营 total=0；RC2 浏览器 `/history?search=683939339441&action=clearance&review_status=approved&execution_state=closed` fresh 唯一返回 1 条，进入 `mode=history` 后保留批次/期间/规则/销售/利润/清仓+禁补及全部事件，返回 URL 恢复四项条件，页面明确只读且无审核、执行、结果或 AI 重试入口 |
| DESIGN-01 | pass | 按更新后的“钉钉协同”同文案 HTML 与生产候选重新生成 1440×900 baseline/actual；首次独立复核发现流程节点系统字符原生违和，改为数据库/规则分支/行动清单/循环闭环四个内联线性 SVG 后重建重截并再次独立看图 | `baseline=/Users/peter123/Documents/work/profit-decision-engine/tests/visual/PAGE-F07-01-baseline.png; actual=/Users/peter123/Documents/work/profit-decision-engine/tests/visual/PAGE-F07-01-actual.png; reviewer=independent-visual; review=pass; blocking=0`；baseline sha256=`409746680355aabcd675aff3560eab4ff927d37e846d512d30c18585a046d605`；actual sha256=`5cf775d9e92cd315fdf66b16ef21f2ec02ad9f8fec1d20e463547e2011e7062d`；真实二维码图案差异为允许项 |
| DESIGN-02 | pass | 以 HTML 明确呈现的 1440×900 会话失效恢复状态生成 baseline/actual；首次独立复核发现 2 项阻塞后改用 SVG 语义图标并恢复双端安全页脚，重建重截并由独立视觉验收官再次实际看图 | `baseline=/Users/peter123/Documents/work/profit-decision-engine/tests/visual/PAGE-F07-02-baseline.png; actual=/Users/peter123/Documents/work/profit-decision-engine/tests/visual/PAGE-F07-02-actual.png; reviewer=independent-visual; review=pass; blocking=0`；actual sha256=`458408b58e4a642f4794ee0b836b84203a4b7860ec5b1854d0e4f3162c254606` |
| DESIGN-03 | pending | 等待与运营工作台 HTML 一致的 SPU 515 当期+前期真实数据后再生成同状态截图并独立看图 | HTML 明确呈现 `BATCH-20260630-01`、`BATCH-20260531-01`、SPU 515 折叠/展开前序；用户真实工作簿只有 2026-06 且无 515，无法以同尺寸/同数据生成 actual；不脑补 baseline 或伪造业务数据 |
| DESIGN-04 | pending | 等待与主管工作台 HTML 一致的待确认清仓、待审核与跨期真实数据后再截图和独立看图 | HTML 明确呈现 `BATCH-20260630-01`、`BATCH-20260707-01` 及待确认清仓完成时间；RC2 仅有用户 XLSX 生成的单期真实批次，其批次码、对象与状态集不同；禁止用前端硬编码复制原型数据 |
| DESIGN-05 | pending | 等待与批次列表 HTML 一致的 11 期真实历史批次后再截图和独立看图 | HTML 明确呈现 `BATCH-20250831-01`至`BATCH-20260630-01` 的当前/历史分页数据；生产 RC2 仅通过用户 XLSX 导入 1 个真实批次，不存在可供同数据比对的历史集；不生成占位批次 |
| DESIGN-06 | pending | 等待含 SPU 515 及 HTML 指定指标/质量状态的真实 2026-06 工作簿后再截图和独立看图 | HTML 明确呈现 `BATCH-20260630-01`、SPU 515、源利润率、7 天品退与 14 天库存口径；用户 XLSX 无 515 且缺可信 7 天品退/库存原始列，生产 API 正确降级为未校验/数据不足，不能改成原型数值 |
| DESIGN-07 | pending | 等待包含原型 14 个 SPU 及 SPU 515 跨期前序的真实批次后再截图和独立看图 | HTML 明确呈现 SPU 389/418/476/515/563/584/632/691/720/728/811/845/905/937 及 2026-05/06 前序；用户 XLSX 的 10 个真实 SPU 集与之不同且无前期文件，无法同数据复现折叠/展开截图 |
| DESIGN-08 | pending | 等待含 SPU 515 的真实跨期建议及原型指定审核/清仓状态后再截图和独立看图 | HTML 明确呈现 SPU 515、`BATCH-20260531-01`、`BATCH-20260630-01`、清仓+禁补与确认层；用户 XLSX 不含 515 且只有单期，RC2 不存在该真实对象/时间线；不用其他 SPU 缩水代签 |
| DESIGN-09 | pending | 等待与历史页 HTML 一致的 2026-04/05/06 真实批次、SPU 集和审计异常后再截图和独立看图 | HTML 明确呈现三期批次与 SPU 476/515/632/720/728/811/845/905；RC2 只有用户 2026-06 单期真实数据，虽已完成组合查询和只读详情 E2E，但不能作为同数据视觉对比证据 |
| RULE-01 | pass | 对 4 组冻结快照执行每组 3 次串行、24 次并发重放，覆盖销售/利润/新品阈值边界；真实 XLSX 批次完成后重建 Worker 3 次，验证不重领完成任务且冻结决策 JSON 逐字节一致 | `TEST_DATABASE_URL=postgres://…/candidate_tests_1936 TEST_XLSX_PATH=…/商品链接.xlsx go test -race -count=1 -v ./internal/batch` exit 0；`TestDecisionRuleBoundaries` 4/4 子用例 pass；`TestDecisionIsStableAcrossSerialAndConcurrentReplay` pass；`TestBatchLifecycleAgainstPostgresAndRealWorkbook` pass（3 次 Worker restart 均 `processed=false`，10 条决策重放前后 JSON bytes 相等） |
| RULE-02 | pending | 已完成双角色/越权矩阵、钉钉企业内部机器人协议外层与消息正文白名单负向扫描；等待真实外部相关人员收件及任务引用直接访问拒绝证据 | `go test -race -count=1 ./cmd/... ./internal/...` exit 0；钉钉消息正文机械断言只含 SPU/链接、协同动作、责任运营、反馈要求和任务引用，利润/推广/品退/售后/阈值/审核/应用密钥命中 0；协议外层精确为 `robotCode/userIds/msgKey/msgParam`，收件人为公司 User ID。2026-08-05 01:18 当前凭据取 token 成功，但无实际机器人编码的官方负向请求返回 HTTP 400 `invalidParameter.robotCode.notExsit`；仍缺机器人编码、外部收件账号与真实收件证据，故保持 pending |
| RULE-03 | pending | 等待可轮换的钉钉与 LiteLLM 测试密钥、运维轮换权限和旧密钥失效观测窗口 | 已执行候选前端产物、API 响应、Compose 启动日志和业务页面扫描，未见密钥明文；生产镜像不含 Node，普通用户无配置入口；用户更换 Client ID/Secret 后已强制重建隔离候选，新凭据的钉钉 Access Token 接口 HTTP 200/有效期 7200 秒，且候选 api/web 日志对新 Secret/LiteLLM API key 值扫描命中 0、token 标记命中 0，本轮跟踪差异对上述值扫描命中 0；但仍缺 `DINGTALK_ROBOT_CODE`、可替换的哨兵密钥、运维轮换权限和旧密钥失效观测窗口，不能伪造轮换成功，故保持 pending |
| RULE-04 | pass | 对 LiteLLM 全故障矩阵执行失败关闭，并以真实网关验证越界/超时后追加合规版本且冻结业务不变 | `TestClientFailsClosedForGatewayAndContentFaults`、`TestClientFailsClosedForConnectionRefusalAndTimeout`、`TestProcessorAppendsFaultVersionsWithoutChangingFrozenDecisionOrTask`、`TestProcessorPersistsValidatedExplanationWithoutChangingBusinessState` 及新增生产提示约束测试均在 `go test -race ./internal/explanation -count=1` 中 pass；真实批次首轮模型失败未阻断 batch=`ready`（10 有效/0 拒绝/10 降级/1 警告），产品重试 API 只追加解释版本并恢复至 actionable latest `generated=7/7`，结构/动作校验错误均为 0，`decision_record` 和 `spu_action_task` 摘要前后一致，密钥未出现在日志 |
| RULE-05 | pass | 在 00001–00005 schema 的隔离生产 Compose 中用真实 XLSX 建立真实批次，并执行包含跨周、改判、审核、执行、结果、清仓、钉钉成功/失败/补发和并发冲突的 PostgreSQL 事件链；备份数据库与 XLSX 后恢复到全新 Compose，再逐项比较关联和新增 User ID/通知字段 | 源 `profit-decision-dingtalk-gate` 生产 API 导入真实 `商品链接.xlsx` 为 1 个 ready 批次/10 SPU，随后 `TEST_DATABASE_URL=…:55441 go test -race -count=1 ./internal/action` exit 0；`backup.sh` 生成 `/tmp/profit-rule05-backup-o4AGET/20260804T162550Z`，三项 SHA256 校验 OK；全新 `profit-decision-rule05-restore` 先迁移到 00005，`restore.sh` exit 0 `batch_count=22 xlsx_file_count=1`；源/恢复计数逐项相等：batch=22、snapshot=31、decision=31、task=28、link=28、event=73、result=3、clearance=4、notification=10、role=4、非空 `dingtalk_user_id`=3；恢复后 10/10 通知 payload 含 `recipient_user_id`，真实 XLSX sha256=`af37a29543bf5a37117985e70619bee2534190a22655359d6e005ea80058cf5f`，恢复 API 读取 `BATCH-20260630-4A34C09E` 成功；二次恢复对非空 `user_session` exit 3 明确拒绝，未覆盖现有数据 |
