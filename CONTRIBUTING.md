# 贡献指南

感谢参与！无论你是业务、开发还是 AI 助手，改这个项目前请先看这里。

## 一、想改东西？先开 issue

1. 去 **Issues → New issue**。
2. 选模板：
   - 想要新功能或改动 → **提需求或改动**；
   - 出错了 → **报 Bug**。
3. 填完提交，不要提空白 issue。

## 二、怎么改

```bash
# 1. 拉取最新 main 并创建分支
git pull
git checkout -b feat/<简短描述>

# 2. 完成改动和验证后提交
git add <相关文件>
git commit -m "简明描述本次改动"

# 3. 推送分支并创建 Pull Request
git push origin feat/<简短描述>
```

分支命名：

- `feat/*`：新功能；
- `fix/*`：修 Bug；
- `docs/*`：文档；
- `refactor/*`：不改变行为的重构。

## 三、PR 合并条件

1. PR 描述清楚改了什么、为什么改，并关联 issue。
2. 完成与本次改动匹配的验证；启用 CI 后必须保持检查全绿。
3. 至少由 CODEOWNERS 复核；只有单一维护者时，按仓库当前保护规则执行。
4. 行为、产品文档、设计与测试保持一致。

## 四、协作铁律

- 不在 `main` 上直接开发，日常改动走分支和 PR。
- 禁止 force-push 到 `main`。
- 改了需求或行为，就同步对应文档和测试。
- 提交应小而可回滚，不混入无关格式化或重构。

## 五、外部贡献者

没有仓库写权限时使用 fork + PR：fork 本仓库，在 fork 的分支完成改动，再向本仓库发起 Pull Request。
